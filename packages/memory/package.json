{
  "name": "@luna/memory",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:bun": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "bench:recall": "bun bench/paraphrase-recall.ts",
    "bench:memory": "bun bench/memory-suite.ts",
    "bench:rerank": "bun bench/rerank-eval.ts",
    "eval:locomo": "bun src/adapters/locomo-eval/run.ts",
    "eval:locomo:judge-rescore": "bun src/adapters/locomo-eval/judge-rescore.ts",
    "bench:bulletin": "bun bench/bulletin-eval.ts",
    "bench:bulletin-generate": "bun bench/bulletin-generate.ts"
  },
  "dependencies": {
    "@luna/core": "workspace:*",
    "effect": "4.0.0-rc.111",
    "vectorlite": "^0.2.0"
  },
  "devDependencies": {
    "@luna/adapter-sdk": "workspace:*"
  }
}
