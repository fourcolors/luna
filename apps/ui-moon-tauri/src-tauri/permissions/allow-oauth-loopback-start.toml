# Generated for the connector OAuth flow (PRD A §09) — see main.rs.
[[permission]]
identifier = "allow-oauth-loopback-start"
description = "Allows the oauth_loopback_start command"
commands.allow = ["oauth_loopback_start"]
