# Redock a pinned chat floater back into its owner window (issue #380).
[[permission]]
identifier = "allow-redock-thread"
description = "Allows the redock_thread command - focus owner, emit redock-thread, close caller"
commands.allow = ["redock_thread"]
