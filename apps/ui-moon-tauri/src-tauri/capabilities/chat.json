{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chat",
  "description": "ADDITIVE permission set for chat widget windows (registry kind 'chat'). Both the singleton `panel-chat` and parameterized `panel-chat-*` labels are covered so every chat panel can use machine access. The panel-* capability already grants native independent window movement; this adds attachments, local shell access, thread persistence, voice, and launching other widgets.",
  "windows": ["panel-chat", "panel-chat-*"],
  "permissions": [
    "dialog:allow-open",
    "allow-local-shell-exec",
    "allow-get-last-thread-id",
    "allow-set-last-thread-id",
    "allow-get-panel-last-thread",
    "allow-set-panel-last-thread",
    "allow-open-widget",
    "allow-open-artifact-widget",
    "allow-voice-status",
    "allow-voice-ptt-down",
    "allow-voice-ptt-up",
    "allow-voice-cancel",
    "allow-speak-text",
    "allow-voice-stop-speaking",
    "allow-notify"
  ]
}
