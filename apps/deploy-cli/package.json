{
  "name": "@luna/deploy-cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile --outfile=dist/deploy-cli src/main.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "citty": "^0.1.6"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "^2.1.5"
  }
}
