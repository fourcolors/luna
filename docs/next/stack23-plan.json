{
  "changelog": [
    "CORRECTION 1 (BLOCKING, S07 ExecStart bricks hosts) -> FIXED per ruling R2. The launcher is now scripts/luna-chat-server-entry.ts, a TypeScript file at a permanent path, and both templates render ExecStart=${bun_bin} run scripts/luna-chat-server-entry.ts (launchd: the same as an argv array). One language, no bash parsing. I re-measured the failure and it is WORSE than the audit found: `bun run` on a `#!/usr/bin/env bash` file with `set -euo pipefail` printed `bun: command not found: set` and EXITED 0 - bun runs it through its own shell, so the systemd failure mode is a silent immediate exit-0 with no READY=1, not a diagnosable exit 1. S07's negative test is therefore a STATIC assertion (rendered ExecStart target ends in .ts and that file is not a shebang script) plus a no-READY runtime assertion, because an exit-code-only test would pass against the broken shape. I also verified the launcher design empirically on this Mac: `bun run entry.ts` where entry.ts awaits an imported `bootstrap()` runs it in the SAME PID (required for Type=notify + WatchdogSec=90) and leaves `import.meta.main === false` in the imported module, while `bun run daemon.ts` directly leaves it TRUE - both paths boot exactly once, neither double-boots. That requires exporting the existing `bootstrap` const at chat-server.ts:4718 (one token, zero runtime change), now listed in S07's files. Two sub-claims in the correction are themselves corrected against the source: the launchd plist DOES already carry PATH=$(dirname $bun_bin) in EnvironmentVariables (launchd-plist.sh:64-65) and `$bun_bin` is absolute in both templates, so bun resolution needs no change; but the plist has no LUNA_REPO_ROOT while luna-server-install:347 writes one, so S07 adds it to the plist or the macOS wake workspace path silently moves when WorkingDirectory changes.",
    "CORRECTION 2 (BLOCKING, S01's stated proof is false) -> FIXED per ruling R1. S01's approach is rewritten around a four-case rename(2) classification measured on this Mac, which the helper must carry as a comment table: CASE 1 symlink-onto-existing-symlink EXIT 0 atomically repointed (guardian:1141, update-server:757/768); CASE 2 directory-onto-vacated-name EXIT 0 plain rename (update-server:706/711/712); CASE 3 directory-onto-non-empty-directory EXIT 1 refused loudly with destination intact (the safety property that beats `mv -fh`, which returns 0 and silently nests); CASE 4 directory-onto-symlink-to-directory EXIT 1 ENOTDIR, loud, and UNREACHABLE at all six sites. The old narrative's claim that perl 'replaces a symlink-to-dir atomically, exactly GNU mv -fT' is deleted. Acceptance and verification now require a fixture exercising all four cases so the table is machine-checked rather than asserted.",
    "CORRECTION 3 (S07 has no rollback for the unit render) -> FIXED per rulings R3 and R2. S07 now copies each installed artifact aside in place before writing (luna-chat-server.service.prev, com.user.luna-chat-server.plist.prev; neither supervisor loads those suffixes, and acceptance asserts zero `.prev` entries in `systemctl list-unit-files`), and the copy-pasteable restore sequence - restore .prev, daemon-reload, reset-failed, restart, or bootout/bootstrap on launchd - goes in the PR body and is exercised once on dev. The demanded fixture for the `luna_active_ws_count` non-zero-exit branch (verified at luna-guardian:525-526) is now an explicit acceptance clause alongside the >0 branch. I also found and closed a hole neither the draft nor the audit named: a pre-S07 release has no launcher file, so the new unit cannot boot the rollback target that do_rollback_releases selects ($PREV, luna-update-server:1723-1740). unit_paths_current now reports 'not current' only when BOTH `current` and `previous` contain the launcher, making the flip self-gating instead of leaving a one-window unbootable rollback.",
    "CORRECTION 4 (S20 Moon test scope understated ~70%) -> FIXED with re-measured numbers. `grep -rln 'panel.html\\|index.html' apps/ui-moon-tauri/test` returns 28 (identical for the escaped and unescaped patterns, so the plan's own pattern gives 28 too); the suite is 39 .ts files totalling 15,602 lines. S20 now states 28 and enumerates all sixteen previously-unnamed files. Risk 7 is corrected from '~9200 lines / 17 files' to '15,602 lines / 39 files / 28 coupled'. Per ruling R6 the S15 re-estimation checkpoint is re-derived from the measured baseline and is now an acceptance clause on S15: record how many of the 28 S15 actually touched and extrapolate before opening S16.",
    "CORRECTION 5 (vendor-layer completeness; only 1 of 13 scripts disposed) -> FIXED per ruling R6 with a measured per-file manifest in S20's approach and acceptance, each disposition proven by `grep -rln 'vendor/<file>' .../frontend-react/*.html .../frontend/*.html`. KEPT-AS-FROZEN-DEP: moon-markdown.js (audited sanitizer, Operator hard constraint) and highlight.min.js (third-party vendored highlight.js; moon-markdown.js:214-241 reads window.hljs inside enhanceCodeBlocks, so it cannot become an ESM import without editing the frozen sanitizer). KEPT-NON-CHAT with named surfaces: moon-protocol.js / moon-ws.js / moon-appearance.js / moon-session.js -> frontend-react index+panel+widget; moon-dock.js / moon-resize.js / moon-window-float.js -> frontend-react panel+widget; pool-engine.js -> frontend-react/index.html:41 (the hub) plus test/pool-engine.test.ts and test/moon-hub.test.ts:36. REPLACED-BY-REACT: ui-transport.js (S18) and capabilities.js - newly resolved from the draft's 'keep for now', because chat.html is its ONLY html consumer and it is generated from packages/capabilities by scripts/bundle-capabilities.ts, so S20 deletes the bundle, the generator, root package.json:20 and test/capabilities-staleness.test.ts. thread-drag-session.js is chat-only and byte-frozen, so S20 `git mv`s it into the bundle with zero content change (provable via `git show --stat -M`), preserving S17's byte-identity guarantee while removing the vanilla vendor copy. End state asserted mechanically: chat.html carries exactly two vendor script tags. S18 now states pool-engine.js's disposition explicitly, and I corrected a factual error in S18's own text - it claimed test/pool-engine.test.ts reads the deleted vendor file, but that test reads vendor/pool-engine.js, a different file that is NOT deleted; only test/ui-transport-staleness.test.ts needed retiring there, and pool-engine.test.ts is removed from S18's file list.",
    "CORRECTION 6 (S25 silently redefines the charter escape hatch) -> FIXED per ruling R7, defaulting to charter compliance. S25 is retitled and now KEEPS a minimal bash disaster-recovery script, scripts/luna-recover, whose entire documented capability is three steps - stop the unit, flip `current` to the release `previous` names, start the unit - with no git, install, build, registry, journal or health logic, and no dependency on the binary (it exists for when the binary will not run). Acceptance caps it at 80 lines and requires it to be EXERCISED on a non-production profile. The engine-pin rollback stays as the second, stronger hatch and is also exercised. The full-4,736-line-deletion variant is RECORDED in docs/deploy-binary.md as an Operator decision pending sign-off, the same treatment S21 gives its architectural decision, and is not executed. The correction's second half is also fixed: S24's luna-registry.sh / luna-doctor problem is promoted from a risk note to a HARD PRECONDITION with its own acceptance and abandon clauses, since luna-doctor is one of three live consumers and stays bash.",
    "CORRECTION 7 (S08 violates its own one-concern rule) -> FIXED per ruling R5. S08 now carries git mv plus exactly two hand-written artifacts (apps/server/package.json and the regenerated bun.lock, both mechanically required by CI HARD GATE 1 `bun install --frozen-lockfile`) plus one tsconfig exclude line; acceptance asserts scripts/, test/ and README.md are untouched. tsconfig.judgeprobe.json is gone from files, approach and verification - it no longer exists (a judge-run artifact, not repo code). The rename-test and README work is NOT deferred to S09 as the ruling suggested, because reading the file showed a fact the ruling did not have: apps/ui-web/scripts/__tests__/rename-chat-server.test.ts asserts that `scripts/luna-server-install` contains the literal `run scripts/chat-server.ts`, so S07 breaks it too, and deferring to S09 would leave a BLOCKING vitest gate red across two merged PRs. It is instead scheduled EARLIER as new prep slice S06a, which retires the completed-2024-rename guard's ~40 path literals down to the live invariant it still earns (the extinct `dev-server-chat` name) and fixes README.md:301. That keeps the ruling's decision intact - S08 carries only the mechanical three - and makes S07 clean as well. This is the only slice added; count is 27, within the +2 budget.",
    "CORRECTION 8 (S07 permanent branching for a transient migration) -> FIXED per ruling R4. The two-way entry probe is kept, but scripts/luna-chat-server-entry.ts carries a `TODO(#issue):` marker directly above it naming the removal condition (every profile's `previous` resolves post-S08) and stating fix-or-remove, matching the repo's annotate-bad-code-in-place convention. S26 gains an explicit retire step in both approach and acceptance: verify the readlink evidence per profile, delete the apps/ui-web branch, and assert `grep -c 'apps/ui-web' scripts/luna-chat-server-entry.ts` is 0 with zero TODO markers remaining.",
    "PASS-THROUGH (ruling R8): S02, S03, S04, S05, S06, S09, S10, S11, S12, S13, S14, S16, S17, S19, S21, S22, S23 are unchanged verbatim. S15 gained only the measured re-estimation clause; S18 gained only the pool-engine disposition and the pool-engine.test.ts correction. Ordering is unchanged; S06a is inserted between S06 and S07 and every existing id is stable.",
    "TOP-LEVEL: sequencingRationale now covers S06a and the corrected S07 launcher with its same-PID proof and the previous-release gate; evalPlan adds the S01 four-case fixture, the S07 static-plus-no-READY negative test and .prev restore drill, and the S20 thirteen-file manifest gate; docsPlan reassigns the README:301 edit from S08 to S06a and rewrites the S25 entry for the retained luna-recover plus the recorded pending decision; risks 0, 1, 7 and 8 are updated with measured numbers and the new gates, and risks are added for the S25 sign-off dependency and for the vendor layer surviving outside chat.",
    "ROUND 2 (orchestrator-applied, 2026-08-03): all nine re-audit corrections applied - S07 gains test/deploy-scripts.test.ts (five unit-shape assertions named) and scripts/lib/port-guard.sh (daemon-identity glob widened to *chat-server*); plutil verification uses a tempfile; S24 parity diff points at the real 'check' subcommand; the bun-run exit-0 claim is qualified to the .sh-extension case everywhere it is operative; S01 updates the :1598 preview string so its grep passes at zero; S03 marks layer.test.ts NEW; S12's grep adopts S26's live-docs scoping; risk 12 records the already-landed defineToolPackage factory (#417-#423). S25 amended to the FULL-DELETION variant per explicit Operator approval (Mr. Cobb, 2026-08-03): no luna-recover script; docs/ops/deploy-recovery.md carries both exercised manual procedures; NEXT.md records the adjudication.",
    "PLAN AMENDMENT (orchestrator, 2026-08-05, after an S08 abandon): the pure-rename S08 spec was unimplementable as written. runtime-paths.ts and setup-login.ts sit inside the daemon closure AND are imported by relative path from seven S10-listed ops CLIs - daily-brief-install, doctor-workflow-run, dream-wake-install, luna-doctor-workflow, push-through-install, vault-migrate-keychain (all `./runtime-paths.js`) and seed-default-account (`./setup-login.js`) - so S10's own premise that these CLIs are 'verified as outside chat-server's import closure' was false. S08 is retitled to move the daemon AND these seven CLIs together (the apps/server/scripts end-state S10 was always heading to), each CLI's relative import rewritten to `../src/<module>.js`; S10 shrinks to the remainder (enable-wake, install-claude-skills, mcp-cli, seed-probe-widgets, dev-server, mcp-demo) and its false-premise sentence is corrected. Four transient dual-path sites are added, each with a TODO(#440) marker mirroring scripts/luna-chat-server-entry.ts's pattern and retired at S26: scripts/luna-update-server's dream_wake_install_script() helper (used at all three call sites that named the daemon path - the two dry-run trace lines AND the real post-deploy seed_dream_wake_jobs() invocation, since leaving the real path unprobed would silently degrade dream/wake seeding to a warning on every deploy), scripts/luna-server-install's seed_default_account_script() helper, and a fourth site in the same file - the inline sd-notify version-skew guard at :459, which after the move must accept both apps/server/src/sd-notify.ts and the ui-web fallback, because leaving only the old path would emit a false start-timeout warning on every post-S08 install. During tracing, __tests__/external-mcp-app-registry.test.ts (co-located with the moving external-mcp-app-registry.ts) turned out to import example-mcp-ui-server.ts, a file S10 had classified as a dev-only 'do not move' demo file, purely as a stdio test fixture with no other consumer in the repo; moving only two of the three would have forced a content edit outside the enumerated categories, so example-mcp-ui-server.ts moved into apps/server/src alongside its dependent test as a pure rename, and S10's file list no longer names it. test/releases-layout.test.ts:505's fixture never materializes an apps/server/scripts/dream-wake-install.ts (it seeds only file.txt, bun.lock and scripts/lib/luna-deploy.sh), so the new probe's not-found fallback resolves to the pre-move literal that assertion already expects; the test passed unchanged (43/43) and needed no edit.",
    "S08 FOLLOW-UP DISCOVERY (implementer, 2026-08-05): a full-vitest run after the move deterministically failed two packages/core tests (job-ticker.test.ts and doctor-enqueue.test.ts, doctor auto-enqueue Phase B1) that a repo-scoped grep during S08's own tracing had missed, because that grep was run from inside apps/ui-web/scripts and its `../..` search root only reached apps/, never packages/. packages/core/src/doctor/doctor-enqueue.ts:47's resolveDoctorCliPath() hardcodes `join(cwd, 'apps/ui-web/scripts/luna-doctor-workflow.ts')` as its DEFAULT when neither an explicit cliPath nor LUNA_DOCTOR_CLI is set, and chat-server.ts's own JobTicker wiring passes neither, so the daemon's real doctor auto-heal path was relying on this exact default; moving luna-doctor-workflow.ts left it silently degrading every doctor auto-enqueue to `doctorCliReachable()==false` -> 'CLI not reachable' -> skip, not a crash, which is why no boot-smoke test caught it. Unlike the deploy-engine probes (transient, tree-shape-ambiguous across rollback because the engine is NOT versioned with the release), packages/core ships INSIDE the same commit as chat-server.ts, so each release's own default always matches its own tree layout - no dual-path probe is needed here, only a straight literal correction. First reverted on scope grounds (packages/core was not among S08's enumerated content-edit categories, so the r3 fixer stopped and reported it as an open blocker rather than force the fix). Re-applied and folded into S08's FILES IN SCOPE by an explicit orchestrator scope grant (slice S08v2-ext, 2026-08-05), fixed as four one-line edits: packages/core/src/doctor/doctor-enqueue.ts:47 (default literal), packages/core/src/doctor/doctor-enqueue.test.ts (two assertions pinning the old default/an explicit happy-path cliPath, plus an existsSync assertion on the resolved default so the next relocation cannot reintroduce this exact blind spot silently), packages/core/src/jobs/job-ticker.test.ts (one explicit happy-path cliPath), packages/core/src/jobs/job-ticker.ts (a doc-comment literal). packages/adapter-sdk/src/workflow-worker.ts:420 and packages/secret-tools/src/register-secret.ts:23 carry similar old-path references but are prose comments only (no existsSync/resolution logic), left as accepted stale-comment debt like the other doc cross-references this slice did not chase."
  ],
  "docsPlan": "Docs update in the slice that changes the thing, not deferred wholesale - both reviewed plans deferred all path truth to their final slice, which made their own S09/S12 verification greps unsatisfiable on live docs (AGENTS.md:23, PROJECT.md:11, README.md:301, docs/HOW_TO_TEST_AND_VERIFY.md:107 all name paths those slices moved).\n\nPer-slice: S03 records the rejected env-var backend selector in DESIGN.md so the decision is on the record. S06 updates docs/next/luna-db-contract.md with the recorded baseline and flips the test:bun CI comment. S06a fixes README.md:301, which documents invoking the rename-guard test that slice rewrites (this edit moved out of S08, which is now renames-only). S09 updates docs/HOW_TO_TEST_AND_VERIFY.md:107, which documents a smoke file that slice deletes. S10 updates AGENTS.md:23 and PROJECT.md:11 alongside the CLI relocation. S12 closes TODO.md:31 (watercolor-token duplication), which the design-system deletion resolves. S21 writes docs/deploy-binary.md carrying the architectural decision requiring Operator sign-off (binary owns the state machine; ServerUpdateDriver gains no methods). S25 documents THREE things in docs/deploy-binary.md: the retained scripts/luna-recover with its exact three-step capability and its explicit non-capabilities, the copy-pasteable engine-pin rollback command, and the recorded-but-unexecuted full-bash-deletion variant awaiting Operator sign-off.\n\nS07 is a docs-in-the-PR-body slice rather than a docs-file slice: the .prev unit restore sequence and the per-profile `previous` readlink evidence are recorded in the PR body, because they are operational runbook steps tied to one rollout, not standing documentation.\n\nS26 is truth-up, not catch-up: NEXT.md Stack 2/3 sections marked against what actually landed, DESIGN.md architecture sections, TESTING.md, and deletion of any live doc describing the ui-web SPA or the vanilla Moon chat. The closing grep is scoped to LIVE docs only (README, DESIGN, AGENTS, PROJECT, TESTING, NEXT, docs/HOW_TO_TEST_AND_VERIFY.md, docs/next/). Archival records under docs/superpowers/plans, docs/briefs, docs/audits and docs/discussions are excluded by design - roughly 20 of the ~35 files matching `apps/ui-web` are historical records that correctly describe what was true when written, and rewriting them to satisfy a gate would destroy the record. Nothing destined for any PR body names a personal host: fourcolors/luna is public.",
  "evalPlan": "Three standing gates run on every slice: `bun run test` (blocking vitest), `bun run test:bun` (promoted to blocking in S06), and `bun run test:hostenv` (locally runnable from S01 onward; still continue-on-error in CI, so treat red as blocking by convention on any deploy slice). `bun run typecheck` is promoted to blocking in S26 once S09 has held apps/server at zero.\n\nAtomic-replace fixture, S01: a four-case table test asserting the measured rename(2) semantics - symlink-onto-symlink EXIT 0 with the link repointed, directory-onto-vacated-name EXIT 0, directory-onto-non-empty-directory EXIT 1 with destination contents intact, directory-onto-symlink-to-directory EXIT 1 - so the helper's comment table is machine-checked rather than asserted in prose. The two failure cases assert destination state, not just exit code, because the whole reason perl beats `mv -fh` is that `mv -fh` returns 0 while corrupting the tree.\n\nLauncher gates, S07: the negative test is STATIC plus behavioral, deliberately not exit-code-only. Measured on this Mac, `bun run` on a `#!/usr/bin/env bash` file containing `set -euo pipefail` prints `bun: command not found: set` and EXITS 0, so an exit-code assertion would pass against the exact broken shape the audit found. The test therefore asserts (a) the rendered ExecStart target ends in `.ts` and the file at that path does not begin with a shebang, and (b) a bash-shebang launcher fixture never emits the daemon's ready marker. Positive tests: the launcher boots a pre-move tree and a post-move tree to a listening 4753 with /readyz green, in ONE process (daemon PID equals launcher PID, which Type=notify plus WatchdogSec=90 make load-bearing). Guardian fixtures cover three reconcile branches - idle (renders exactly once), active sessions >0 (defers), and `luna_active_ws_count` NON-ZERO EXIT (the 'session count unknown' branch at luna-guardian:525-526) - plus a fourth where `previous` lacks the launcher and the old unit must be left installed. The .prev unit restore is exercised once on dev with the transcript attached.\n\nMemory swap eval, S04: one shared `runMemoryBackendContract` runs against both real backends, plus the actual swap proof - a second backend that implements `search` (satisfying MemoryVectorBackend across vec|hybrid|bm25|hybrid-terms) driven end to end through `makeMemoryRouterLayer` so memory_search returns ranked results with a zero-line production diff. Deliberately test-only: shipping a second production backend with one consumer is the speculative abstraction NEXT.md decision 2 forbids. Negative control: breaking one backend's delete() must fail only that backend's contract run. Retrieval quality is guarded separately by `bun run --filter '@luna/memory' bench:memory`, whose recall baseline is RECORDED in S06 against a copy of the real memory.db and re-compared in S26 with a 5% abandon band.\n\nDaemon cutover gate, S06 (before the move, per NEXT.md): the docs/next/luna-db-contract.md 3-step run against COPIES - clean no-op migration boot, bench replay, job-reconciliation smoke with an orphaned job_runs row and a sticky jobs.last_status='running' row. S08 re-runs test:bun as a hard abandon condition.\n\nDeploy gates: S07 and S11 are gated on per-profile engine-pin advancement (`readlink previous` post-S07; pinned engine SHA post-S11), verified per host before the dependent slice merges - not on a prose \"deploy window\". S22/S24 are gated by a golden parity harness diffing exit codes, the literal `ROLLED BACK to` stderr marker, and on-disk state-file BYTES, including crash-injection between journal phases and bidirectional bash/binary interop. S25 is gated on BOTH escape hatches being actually exercised on a non-production profile: the retained scripts/luna-recover stop/flip/start, and an engine-pin rollback.\n\nMoon gates: `bun run test -- apps/ui-moon-tauri/test/ws-contract.test.ts` (CI HARD GATE 5) on every Moon slice; `git show --stat -M` byte-identity on moon-markdown.js, highlight.min.js, widget-sandbox.js and thread-drag-session.js (the last surviving a `git mv` into the bundle in S20); `cargo build` in S20 for the include_str! path; real-Tauri screenshots per surface, with S17's drag held for manual Operator exercise because feel cannot be screenshot-proved. S20 adds a mechanical vendor-layer gate: the thirteen-file disposition manifest is reproduced in the PR body with its grep output, and `grep -c '<script src=\"/vendor/' chat.html` must equal 2. S15 carries a measurement gate rather than a pass/fail one: the Moon suite baseline is 39 test files, 15,602 lines, 28 of them referencing panel.html or index.html, and S15 records how many it actually touched so S16-S20 are re-estimated from data.\n\nFinal, S26: full-agent E2E through Moon against a deployed apps/server daemon - streaming, tool call, every artifact kind, memory write and recall, job creation delivered into the thread, drawer drag-out, slash commands - plus one binary update with a forced rollback, plus retirement of S07's transient entry probe with the per-profile readlink evidence recorded.",
  "risks": [
    "S07 is a real flag day and no plan can remove it, only bound it. Verified: `grep -n 'luna-server-install' scripts/luna-update-server` hits only comments (18/31/462/466/474); do_rollback (1780) and do_rollback_releases (1723) flip the tree and restart the SAME unit without re-rendering. Two distinct exposures, now separately bounded. (a) A BAD RENDER: mitigated by S02 landing first (reset-failed clears the start limiter so it self-heals rather than locking out for 30 minutes) and by the new in-place `.prev` copy-aside with a documented restore sequence. (b) A GOOD RENDER OVER A STALE ROLLBACK TARGET: a pre-S07 release contains no scripts/luna-chat-server-entry.ts, so the new unit cannot boot the release do_rollback_releases flips to ($PREV). Mitigated by making unit_paths_current report 'not current' only when BOTH `current` and `previous` carry the launcher, so the flip is self-gating and never leaves an unbootable rollback target. Dev-profile-first with a two-cycle soak still applies.",
    "Guardian's reconcile path has a documented no-self-heal branch: if `luna_active_ws_count` exits nonzero, reconcile_unit_if_idle warns 'session count unknown; deferring' and returns 1 (luna-guardian:525-526). A down or start-limit-locked daemon is exactly when that probe is most likely inconclusive, so automatic adoption silently will not happen at the worst moment. S07 now carries a fixture for that branch as an acceptance clause, and the operator fallback is the same documented `--units-only` invocation the guardian would have made; the residual risk is that adoption is manual on precisely the hosts that most need it.",
    "The engine-pin lag is a deadlock generator, not just a delay. The deploying engine is a PINNED copy of an older SHA, so pre-S11 engines refuse to build post-S12 releases (release_artifacts_ok hard-requires apps/ui-web/dist/index.html, and the build call is `return 1`, not a warn). If S12 merges before every profile's pin has advanced past S11, `current` never advances, which means the pin never advances either - the fix ships inside the tree the old engine refuses to build. The gate must be per-profile pin-SHA verification, not a calendar window.",
    "Stable runs a 15-minute unattended autodeploy timer tracking origin/master with autoUpdate on, so merging luna-next to master auto-deploys within 15 minutes with no human in the loop. Nothing in this plan flips that flag. Before S07 or S11 reaches master, either the timer is disarmed for the cutover or a per-profile merge fuse is added; a prose deployNote is not a control.",
    "Promotion freshness sits on a knife edge: guardian_status_evidence gates on LUNA_GUARDIAN_HEALTH_WINDOW_SEC (default 900) while stable's timer interval is also 15min = 900s, and the code's own comment warns a cadence at or beyond the window reads every heartbeat as stale and starves auto-promotion. Every Stack 3 slice assumes the new engine eventually promotes. S23 must measure and tune this rather than assume it.",
    "The Stack 3 guardian fold replaces working, heavily-tested bash running as root against production restarts, with no user-visible payoff. Its invariants - atomic writes, re-read-after-write, PID+fingerprint lock liveness, phase-resumable journal, single-evidence-path shared by human accept and auto-promotion, redact() before diagnose captures hit disk at mode 600 - are exactly what a rewrite drops silently. Every slice from S22 is gated by golden parity including bidirectional interop, and S24's redaction abandon condition stops the entire fold rather than risking a token on disk in a public-repo project.",
    "Moon's S19 remains the largest unsplit unknown: roughly 4000 lines across a dozen engines, with the secure secret-entry path (secrets must never enter the transcript) as a non-negotiable property that React state could silently violate. The slice mandates a four-way split and a hard stop on that property, but it is the place a regression is most likely to hide.",
    "MEASURED, not estimated: apps/ui-moon-tauri/test/ holds 39 .ts files totalling 15,602 lines, of which 28 reference panel.html or index.html - not the ~9,200 lines and 17 files the audited draft asserted, and not the 10 one reviewed plan assumed. Every Moon slice implicitly rewrites part of that suite, and ws-contract.test.ts is a blocking CI gate. This is the largest unbudgeted cost in Stack 3; S15 now carries an acceptance clause requiring the remaining Moon sequence to be re-estimated against these numbers before S16 opens, and re-planning S16-S20 is the expected response to a bad extrapolation.",
    "S24's registry consolidation now has a hard precondition rather than a hope: scripts/lib/luna-registry.sh has live consumers in scripts/luna-guardian, scripts/luna-autodeploy and scripts/luna-doctor, and luna-doctor stays bash through S25. Unless S24 either repoints luna-doctor at the binary's registry subcommand or folds it too, the 'one parser' outcome is unachievable and S25 must not delete luna-registry.sh. Discovering this at S25 instead of S24 would mean either shipping two parsers or an unplanned luna-doctor rewrite inside a deletion PR.",
    "S25 now defaults to charter compliance by keeping scripts/luna-recover, which means the fold's deletion payoff is slightly smaller than the audited draft claimed and there is a small bash artifact to maintain. The countervailing risk is real and is why the smallness is capped in acceptance: a recovery script that grows conditionals, health checks or a journal becomes a second engine and defeats the fold. The full-deletion variant remains available but is now blocked on Operator sign-off, so the stack can close with either outcome without re-planning.",
    "The chat vendor layer only partially dies, and that is now explicit rather than implicit. Of the thirteen scripts chat.html loads, eight are KEPT because other Moon surfaces (frontend-react index.html, panel.html, widget.html) load them, and two are KEPT as frozen dependencies inside chat itself (moon-markdown.js and highlight.min.js, which the sanitizer reads as window.hljs). The charter's 'delete the vanilla vendor layer' is satisfied for CHAT - two script tags remain instead of thirteen - but the vendor directory survives for the hub, panels and widgets, and collapsing it is a separate future concern this plan deliberately does not open.",
    "NEXT.md's chat-service / job-ticker / main.rs split is NOT missing from this plan - I verified it already landed (#424 job-ticker, #425 chat-service, #426 main.rs) with the sibling modules present on disk (job-ticker-producer/executor/reconcile, chat-service-tools/thread-lifecycle/sdk-messages/account-rotation, and main.rs alongside lifecycle/windows/connection/updater/oauth/shell/notify). The parent files remain 1556/657/463 lines because the splits extracted along seams rather than shrinking the entry points; no further slice is warranted, and one judge's claim that this scope is unaddressed is rebutted here rather than acted on. The same accounting covers NEXT.md Stack 2's opening item: the defineToolPackage factory + all ten tool-package migrations ALREADY LANDED (#417-#423; all ten packages import defineToolPackage from packages/tools/src/define-tool-package.ts), which is why no slice re-proposes them - Stack 2 coverage is complete."
  ],
  "sequencingRationale": "Verification capability first, then recovery capability, then the one structural change that makes every later move reversible.\n\nS01 is first because 59 of 60 local hostenv failures share one root cause and CI hides the suite (continue-on-error), so without it no Sonnet agent can locally verify any deploy edit in S02, S07, S10, S11 or the whole Stack 3 fold. S02 is second because `systemctl reset-failed` exists nowhere in scripts/ while the unit carries StartLimitBurst=10 over 1800s with RestartSec=5 - ten failed starts exhaust the burst in ~50s, inside the 60s readiness budget, after which rollback's own restart also fails and the host is hard-down for 30 minutes. Every later unit-touching slice needs that net already in place.\n\nS03/S04 are zero-deploy-surface and land before the tree churns so the cutover window is unconfounded. S05 kills SPA static serving before anything moves, deleting the one coupling (`__uiWebDir` resolving one level up) that degrades silently rather than crashing; deleting it also removes any need for transitional `LUNA_UI_WEB_STATIC_ROOT` wiring in the unit templates, and eliminates the add-then-remove churn both reviewed plans inflicted on production systemd and launchd files. S06 runs the luna.db gate BEFORE the cutover, as NEXT.md line 42 requires; both reviewed plans ran it at the very end, after the cutover it governs.\n\nS06a is a new one-file prep slice and it exists because of a fact only reading the test surfaces: apps/ui-web/scripts/__tests__/rename-chat-server.test.ts is a blocking-gate regression guard for a COMPLETED 2024 rename, and three of its assertions pin `scripts/luna-server-install` to the literal `run scripts/chat-server.ts` while ~40 more pin the daemon's current path. S07 breaks the first group and S08 breaks the rest, so retiring the path literals down to the live invariant (the extinct `dev-server-chat` name) must precede both, or CI stays red across two merged PRs. Doing it here is what lets S08 be renames-only.\n\nS07 is the pivot and the plan's main structural departure. The supervisor unit is host-persistent state naming a version-dependent path, and I verified nothing re-renders it on rollback - `luna-server-install` appears in luna-update-server only in comments, and do_rollback flips the tree and restarts the same unit. So one unit file must be correct for the new release AND every rollback target simultaneously. A forwarding shim cannot achieve that (it lives in the new tree; a rollback replaces the tree), which is why the reviewed plan's shim ladder leaves a reachable bricked state. A repo-root launcher that probes for whichever daemon entry exists in the release it runs from makes the unit path-independent forever: exactly ONE unit change in the whole plan, and the last one ever needed. The launcher is TYPESCRIPT, not bash: measured on this Mac, `bun run` on a bash-shebang file interprets it with bun's own shell and can exit 0 SILENTLY, which under Type=notify is an immediate no-READY exit and a restart spiral with nothing in the exit code to diagnose. The TypeScript launcher was measured too - it runs the daemon's exported bootstrap in the SAME PID (required by Type=notify plus WatchdogSec=90) and leaves `import.meta.main` false in the imported module, while a direct `bun run <daemon>` leaves it true, so both invocation paths boot exactly once. It is paired with guardian's path-drift check because unit_hardened() inspects only Type and WatchdogUSec and its sole caller is gated on it, so nothing would otherwise adopt a re-render; and the drift check is itself gated on `previous` also carrying the launcher, so the flip can never strand the release do_rollback_releases would select.\n\nS08 then becomes a pure rename with no unit edit and no shim at all - which also sidesteps the reviewed plan's fatal `import.meta.main` defect, where a side-effect-import shim exits cleanly without booting and every host times out silently. With S06a done and tsconfig.judgeprobe.json gone, S08 carries only `git mv` plus the two artifacts CI's `bun install --frozen-lockfile` mechanically requires. S09-S10 clean up behind it. S11 strictly precedes S12 because `release_artifacts_ok` hard-requires apps/ui-web/dist/index.html, and because the deploying engine is a PINNED copy of an older SHA - a pre-S11 engine cannot materialize a post-S12 release, so the gate is engine-pin advancement per profile, not merely a deploy window.\n\nStack 3 starts only after S13. Moon (S14-S20) comes before the control-plane fold because it rides the Tauri updater and cannot brick a host, and because chat.html is 12005 lines with an 8930-line inline script - honest sizing is seven slices with explicit split-on-contact triggers, following panel.html's proven incremental pattern where the page stays bootable at every commit. S15 now carries a measured re-estimation checkpoint because the coupled Moon test surface is 15,602 lines across 39 files, 28 of them tied to the pages S20 deletes. The fold (S21-S25) is last and most gated: S21 must ship as bash first because publish_engine copies six named bash files and runs `bash -n` over the `luna-*` glob, so the publisher must learn about binaries before any binary can be current. S25 exists because a fold that never deletes is a fork - it collects the deletion payoff while keeping the charter's promised bash escape hatch in its smallest honest form, and names the engine pin, already holding five prior working engines on every host, as the second hatch that works when the new artifact is the broken thing. S26 closes the loop by retiring S07's transient entry probe once every profile's `previous` is post-S08, so the migration leaves no permanent branching behind.",
  "slices": [
    {
      "id": "S01",
      "title": "Portable atomic replace via perl rename",
      "concern": "Six GNU-only atomic flips make the hostenv suite unrunnable on macOS, so no deploy slice can be locally verified.",
      "files": [
        "scripts/lib/luna-deploy.sh",
        "scripts/luna-guardian",
        "scripts/luna-update-server",
        "test/deploy-scripts.test.ts"
      ],
      "approach": "Verified six call sites: luna-guardian:1141 and luna-update-server:706, 711, 712, 757, 768; `mv -T` returns 'mv: illegal option -- T' on this Mac. Add ONE helper `luna_atomic_replace <src> <dst>` in scripts/lib/luna-deploy.sh implemented as `perl -e 'rename($ARGV[0],$ARGV[1]) or exit 1' \"$1\" \"$2\"`, and give it a comment table of the four rename(2) cases MEASURED on this Mac - the previous narrative for this slice overclaimed and would have sent an implementer chasing a non-defect, so the proof is now per-case. CASE 1, symlink onto an EXISTING symlink (stage `ln -s` into `$linktmp`, then flip): EXIT 0, the link is repointed atomically - this is exactly luna-guardian:1141 and luna-update-server:757, 768. CASE 2, directory onto a VACATED name (`$aside` is `rm -rf`'d at :705 and `$rel` is vacated at :706, so the destination does not exist at flip time): EXIT 0, plain rename semantics - this is luna-update-server:706, 711, 712. CASE 3, directory onto a NON-EMPTY directory: EXIT 1, refused loudly, destination contents intact - this is the safety property that makes perl beat `mv -fh`, which returns EXIT 0 and SILENTLY NESTS src inside a surviving dst, converting a loud pre-flip failure into a corrupt release tree that still satisfies release_artifacts_ok. CASE 4, directory onto a symlink-to-directory: EXIT 1 ENOTDIR, loud. Case 4 is UNREACHABLE at all six sites - every site either stages a fresh symlink and flips symlink-over-symlink (case 1) or moves a directory into a name vacated first (case 2) - and if a future caller ever reaches it, it fails loudly rather than silently, which is the property that matters. Perl is one code path with no GNU-vs-BSD probe and no cached capability state. Deliberately NOT bun-based: guardian:1141 is the engine-pin publish, the primitive that must work when the bun runtime is exactly what is broken. Preserve the load-bearing comments at luna-update-server:763-765 and every `cd -P` post-flip assertion verbatim. ALSO update the dry-run preview text at scripts/luna-update-server:1598 (printf '7. flip-inside-restart: ... staged ln + mv -fT + cd -P assert') to name the new mechanism ('staged ln + atomic rename + cd -P assert') - it describes exactly the code this slice changes, same concern - so the verification grep returns zero hits with no unstated residue.",
      "acceptance": "`bun run test:hostenv` on macOS emits zero `illegal option` lines, and its pass count is within one failure of the Linux CI run of the same suite. Measure BOTH numbers and record them in the PR body; do not hardcode a target, because the current baseline is not derivable from the repo (test:hostenv is continue-on-error at ci.yml:232 so CI hides it). A fixture exercises all four rename cases and asserts EXIT 0/0/1/1 with destination state intact on both failures, so the helper's comment table is machine-checked rather than promised.",
      "abandonCondition": "If perl is not guaranteed present on a deploy target, abandon and instead skipIf-guard the affected tests on darwin (matching the existing plutil -lint precedent). Never weaken any of the three symlink flips to `ln -sfn`: that is unlink+symlink, not atomic, and is the exact regression class the in-file comments describe. Never substitute `mv -fh` to make case 4 pass: case 4 is unreachable and loud, whereas `mv -fh` turns case 3 into silent corruption.",
      "verification": "grep -rn 'mv -fT\\|mv -T' scripts/ | grep -v ':[0-9]*: *#'  # PASS = zero hits (the :1598 preview string is updated by this slice)\ngrep -c 'luna_atomic_replace' scripts/lib/luna-deploy.sh scripts/luna-guardian scripts/luna-update-server\nbun run test:hostenv -- test/deploy-scripts.test.ts\nbun run test:hostenv 2>&1 | tee /tmp/s01.txt; grep -c 'illegal option' /tmp/s01.txt\nbash -n scripts/luna-guardian scripts/luna-update-server scripts/lib/luna-deploy.sh\nshellcheck --severity=warning scripts/luna-guardian scripts/luna-update-server scripts/lib/luna-deploy.sh",
      "deployNote": "Ships through the engine pin (publish_engine copies lib/*.sh). No behavior change on GNU hosts."
    },
    {
      "id": "S02",
      "title": "Clear the systemd start limiter before every restart",
      "concern": "`systemctl reset-failed` exists nowhere in scripts/, so a failed-start burst locks the unit out for 30 minutes and both guardian repair rungs fail identically.",
      "files": [
        "scripts/luna-update-server",
        "test/update-server.test.ts"
      ],
      "approach": "Verified `grep -rn 'reset-failed' scripts/` returns ZERO hits, while the rendered unit carries StartLimitIntervalSec=1800, StartLimitBurst=10, RestartSec=5, Restart=always (luna-server-install:255-258, 272-273). Ten failed starts exhaust the burst in about 50 seconds, inside the 60s readiness budget, after which `systemctl restart` fails immediately for the remaining ~30 minutes, so the ROLLBACK's own restart fails too and the service stays down with only manual SSH left. Add `systemctl reset-failed <unit>` inside sup_start immediately before the start, systemd only (launchd has no equivalent - say so in a comment). Pure recovery-capability addition, no happy-path behavior change. This must precede S07 because S07 is the only unit change in the plan and this is what turns a bad render from a 30-minute hard outage into a self-healing blip.",
      "acceptance": "A fixture that drives the unit into `failed` via 10 rapid failed starts is recovered by a single `luna-update-server --restart-only` with no manual step; happy-path restart timing is unchanged.",
      "abandonCondition": "If reset-failed masks a genuine crash-loop from the OnFailure alert path, abandon the blanket placement and add it only to do_rollback's restart (luna-update-server:1780), where lockout is unrecoverable rather than merely slow.",
      "verification": "grep -n 'reset-failed' scripts/luna-update-server\nbun run test:hostenv -- test/update-server.test.ts\nbash -n scripts/luna-update-server && shellcheck --severity=warning scripts/luna-update-server",
      "deployNote": "Must be accepted through the engine pin BEFORE S07 lands. This is the recovery net for every later deploy slice."
    },
    {
      "id": "S03",
      "title": "Memory backend by parameter injection, with no env branch",
      "concern": "MemoryRouterLayer hardcodes SqliteVectorBackend, so the backend is swappable at the type level but not at the wiring point.",
      "files": [
        "packages/memory-tools/src/layer.ts",
        "packages/memory-tools/test/layer.test.ts (NEW)",
        "DESIGN.md"
      ],
      "approach": "Verified packages/memory-tools/src/layer.ts:152-158 is `const backend = yield* SqliteVectorBackend; return MemoryLayer({rules:[{pattern:'*',backend}]})` piped through `Layer.provideMerge(SqliteVectorBackend.fromPath(dbPath))`. Add `makeMemoryRouterLayer(backendLayer, backendTag)` and keep `MemoryRouterLayer(dbPath)` as a one-line sqlite convenience wrapper over it; the boot call site is untouched. EXPLICIT REBUTTAL of the LUNA_MEMORY_BACKEND env selector both reviewed plans proposed: I verified packages/memory/src/backends/in-memory.ts:17-30 declares put/get/query/delete/exportAll/importAll and NO `search`, so hasVectorSearch() (backend.ts:62-66) is false and router.ts:193 fails every search with `no vector backends registered`. `LUNA_MEMORY_BACKEND=in-memory` would be a bootable production config that silently kills memory_search. No env var ships. Backend choice is a build-time decision (the on-disk format differs), expressed at one call site - which is also less branching, not more.",
      "acceptance": "makeMemoryRouterLayer takes the backend as an argument; `SqliteVectorBackend` appears in packages/memory-tools/src only inside the convenience wrapper and its import; the boot path is behaviorally byte-identical.",
      "abandonCondition": "If threading the Tag leaks a backend-specific requirement into the router Layer's R-channel beyond the LunaSqliteBootstrap already visible at chat-server.ts:1893, STOP - the seam is not clean and that finding goes to the Operator before any wiring change is made.",
      "verification": "grep -n 'SqliteVectorBackend' packages/memory-tools/src/layer.ts\nbun run test -- packages/memory-tools packages/memory\nbun run test:bun",
      "deployNote": "No deploy surface: library wiring only, and the boot call site is unchanged."
    },
    {
      "id": "S04",
      "title": "Swap-proof: one shared contract, run against a second vector backend",
      "concern": "Nothing mechanically proves a different backend satisfies the same semantics through the router.",
      "files": [
        "packages/memory/test/backend-contract.ts",
        "packages/memory/test/in-memory.test.ts",
        "packages/memory/test/sqlite-vector.test.ts",
        "packages/memory-tools/test/router-swap.test.ts"
      ],
      "approach": "Two halves. (a) Extract `runMemoryBackendContract(label, makeLayer)` covering the exact MemoryBackend surface declared at packages/memory/src/backend.ts:25-37 (backendName/put/get/query/delete/exportAll/importAll plus scope filtering), call it from in-memory.test.ts (154 lines, 5 its) and sqlite-vector.test.ts, and DELETE the now-duplicated assertions rather than merely supplementing them. (b) The actual swap proof: add a test-only backend that DOES implement `search` (satisfying MemoryVectorBackend, modes vec|hybrid|bm25|hybrid-terms) and drive it through makeMemoryRouterLayer end to end, asserting memory_search returns ranked results with zero source changes under packages/memory-tools. Test-only is deliberate: shipping a second production backend with one consumer is the speculative abstraction NEXT.md decision 2 forbids, and the Operator's constraint is a provably clean seam, not a second product.",
      "acceptance": "Both real backends pass one shared contract; a second MemoryVectorBackend serves memory_search through the router with a zero-line production diff; deliberately breaking one backend's delete() fails only that backend's contract run.",
      "abandonCondition": "If SqliteVectorBackend cannot satisfy an assertion InMemoryBackend satisfies, do NOT loosen the contract so both pass - land it with the divergence documented in a comment and reported. A silently-loosened contract proves nothing about swappability.",
      "verification": "bun run test -- packages/memory/test/in-memory.test.ts packages/memory/test/sqlite-vector.test.ts packages/memory-tools/test/router-swap.test.ts\ngrep -c 'runMemoryBackendContract' packages/memory/test/backend-contract.ts packages/memory/test/in-memory.test.ts packages/memory/test/sqlite-vector.test.ts\nbun run test:bun",
      "deployNote": "Test-only; the second backend never ships in production wiring, so there is no deploy surface."
    },
    {
      "id": "S05",
      "title": "Delete SPA static serving from the daemon",
      "concern": "The daemon resolves apps/ui-web/dist through a sibling-directory lookup, the one coupling that fails silently across a relocation.",
      "files": [
        "apps/ui-web/scripts/chat-server.ts",
        "packages/ui-ws/src/server.ts",
        "apps/ui-web/scripts/serve-static.ts",
        "apps/ui-web/package.json",
        "docs/web-client/serving.md"
      ],
      "approach": "Verified chat-server.ts:3001 `__uiWebDir = resolve(dirname(fileURLToPath(import.meta.url)), \"..\")`, computeStaticRoot at 3006-3018, and `staticRoot: computeStaticRoot()` at 4582; plus packages/ui-ws/src/server.ts staticRoot field at 748, serveStatic from 895, and the fetch branch at 1084 and 1152-1155. Delete the path outright rather than re-pointing it: the code's own comment at 2996 says it degrades to `staticRoot = undefined`, i.e. static serving turns OFF with only a log line and no readiness signal - exactly the silent-regression class a relocation must not carry. Deleting also removes the need for any LUNA_UI_WEB_STATIC_ROOT transitional wiring in the unit templates. Cut serveStatic by matching braces, not by a line range: the body extends past 905. Delete serve-static.ts (verified zero consumers beyond its own package.json `serve:static` entry) and docs/web-client/.",
      "acceptance": "/healthz and the WS upgrade path behave identically; GET / no longer serves a page; the ui-ws suite passes with the static-serving specs REMOVED rather than skipped.",
      "abandonCondition": "If removing the fetch-handler branch changes the /healthz response in any way (server.ts:1097 warns the exact-match interacts with staticRoot), land only the chat-server half and leave the ui-ws field unused - a broken /healthz breaks readiness_ok and triggers auto-rollback on every deploy.",
      "verification": "grep -rn 'staticRoot\\|computeStaticRoot\\|__uiWebDir\\|LUNA_UI_WEB_STATIC' packages/ui-ws/src apps/ui-web/scripts\ngrep -rn 'serve-static\\|serve:static' . --include='*.json' --include='*.ts' --exclude-dir=node_modules\nbun run test -- packages/ui-ws\nbun run test",
      "deployNote": "User-visible: :4753 stops serving a web page. Moon is unaffected (it connects over WS). Single-PR revert if wrong. Deploys still BUILD dist at this point; that is removed in S11."
    },
    {
      "id": "S06",
      "title": "Run the luna.db schema-continuity gate before any cutover, and make it blocking",
      "concern": "NEXT.md requires the contract established before any daemon cutover; it landed as docs+test but has never been run against real state and its CI gate is advisory.",
      "files": [
        "docs/next/luna-db-contract.md",
        "packages/core/test/luna-db-contract.test.ts",
        ".github/workflows/ci.yml"
      ],
      "approach": "Run the documented 3-step gate against COPIES of luna.db / memory.db / workspace.db, never the live files: (1) boot the daemon against copies and require a clean no-op migration skip, (2) replay `bun run --filter '@luna/memory' bench:memory` against the copied memory.db and RECORD the recall baseline, (3) the job-reconciliation smoke seeded with an orphaned job_runs row and a sticky jobs.last_status='running' row per the reconcileAfterCrash fixtures. Then promote `bun run test:bun` off continue-on-error (verified at .github/workflows/ci.yml:239) - the contract is worthless as a gate while it is advisory, and ci.yml's own comment names it the next promotion candidate. Both reviewed plans deferred this gate to their final slice, i.e. AFTER the cutover it governs; that inverts the charter's explicit ordering and is corrected here.",
      "acceptance": "All three steps pass against copies, the retrieval baseline number is recorded in the PR body (it is the abandon threshold for S08 and S26), and `bun run test:bun` is blocking and green.",
      "abandonCondition": "If any step shows a non-no-op migration, STOP the entire cutover. That is precisely the surprise the contract exists to catch and no later slice may proceed until it is explained.",
      "verification": "bun run test:bun\nbun run test -- packages/core/test/luna-db-contract.test.ts\ngrep -n 'continue-on-error' .github/workflows/ci.yml\nbun run --filter '@luna/memory' bench:memory 2>&1 | tee /tmp/mem-baseline.txt",
      "deployNote": "Copies only, never live DBs. The recorded baseline is carried forward to the final slice."
    },
    {
      "id": "S06a",
      "title": "Retire the completed-rename guard and its README pointer before any path moves",
      "concern": "A blocking-gate test pins ~40 literal daemon paths, so S07's ExecStart change and S08's git mv would each turn CI red for reasons unrelated to their own concern.",
      "files": [
        "apps/ui-web/scripts/__tests__/rename-chat-server.test.ts",
        "README.md"
      ],
      "approach": "Verified apps/ui-web/scripts/__tests__/rename-chat-server.test.ts (282 lines) is a regression guard for a COMPLETED 2024 rename, `dev-server-chat` -> `chat-server`, and it runs under the blocking vitest gate. Its assertions split cleanly in two. The live invariant: the names `dev-server-chat` and `dev:server:chat` must never come back. The dead weight: roughly 40 path literals that merely restate where files live TODAY - `apps/ui-web/scripts/chat-server.ts` exists, apps/ui-web/package.json's `server:chat` body is exactly `bun run scripts/chat-server.ts`, and three assertions that `scripts/luna-server-install` contains the literal `run scripts/chat-server.ts`. That last group breaks on S07, which changes ExecStart, and the rest break on S08, which moves the file - so leaving them until after the move would leave a blocking gate red across two merged PRs, and folding them into either slice would violate that slice's own one-concern rule. Rewrite the file to assert the live invariant as repo-wide greps carrying no current path, and delete the path literals: `git show --stat -M` in S08 plus S07's launcher boot test are strictly stronger evidence that the move happened than a hardcoded string, and a test that must be edited by every future move is a tax, not a guard. Fix README.md:301, which documents invoking this test, in the same PR. Nothing else moves in this slice.",
      "acceptance": "`bun run test` is green; the rewritten file contains zero occurrences of `apps/ui-web` and zero `chat-server.ts` path literals; deliberately reintroducing `dev-server-chat` anywhere in the repo still fails it; README.md:301 names the test's real invocation.",
      "abandonCondition": "If any deleted assertion is the SOLE guard of a live invariant other than the extinct name, keep that assertion and express it without a path literal instead of deleting it. Do not weaken the extinct-name check to make the rewrite smaller - that check is the only thing this file still earns its place for. If the file turns out to guard something S07 or S08 genuinely needs pinned, stop and report before either slice starts.",
      "verification": "bun run test -- apps/ui-web/scripts/__tests__/rename-chat-server.test.ts\ngrep -c 'apps/ui-web' apps/ui-web/scripts/__tests__/rename-chat-server.test.ts\ngrep -rn 'dev-server-chat\\|dev:server:chat' . --exclude-dir=node_modules --exclude-dir=.git\ngrep -n 'rename-chat-server' README.md\nbun run test",
      "deployNote": "Test and docs only; no runtime file changes and no deploy surface. It exists purely so S07 and S08 each stay one concern."
    },
    {
      "id": "S07",
      "title": "One path-independent unit, forever: repo-root TypeScript launcher plus guardian path-drift detection",
      "concern": "The supervisor unit is host-persistent state naming a version-dependent path, and nothing re-renders it on rollback, so any later path change breaks rollback in one direction.",
      "files": [
        "scripts/luna-chat-server-entry.ts",
        "scripts/luna-server-install",
        "scripts/lib/launchd-plist.sh",
        "scripts/luna-guardian",
        "apps/ui-web/scripts/chat-server.ts",
        "test/releases-layout.test.ts",
        "test/guardian.test.ts",
        "test/deploy-scripts.test.ts",
        "scripts/lib/port-guard.sh"
      ],
      "approach": "This is the structural slice both reviewed plans missed, and it removes the need for the forwarding shim entirely. Verified: `WorkingDirectory=${REPO_DIR}/apps/ui-web` (luna-server-install:266) and `ExecStart=${bun_bin} run scripts/chat-server.ts` (271), with REPO_DIR = the `current` symlink under releases layout; launchd bakes the same pair as an argv array at lib/launchd-plist.sh:41-46. And `grep -n 'luna-server-install' scripts/luna-update-server` hits ONLY comments (18/31/462/466/474) - do_rollback (1780) and do_rollback_releases (1723) flip the tree and restart the SAME unit, never re-rendering it. So one unit file must be simultaneously correct for the new release AND every rollback target in releases/. A forwarding shim cannot cover that: the shim lives in the NEW tree and a rollback replaces the whole tree. Fix the shape instead.\n\nTHE LAUNCHER IS TYPESCRIPT, NOT BASH. `bun run <bash-file>` does not work, and the failure is worse than a parse error: measured on this Mac, a file with `#!/usr/bin/env bash` and `set -euo pipefail` under `bun run` printed `bun: command not found: set` and EXITED 0, because bun interprets it with its own shell. Under Type=notify that is an immediate clean exit with no READY=1, so TimeoutStartSec=60 expires and every host restart-spirals with nothing in the exit code to diagnose. Add scripts/luna-chat-server-entry.ts, resolve the daemon entry RELATIVE TO `import.meta.url` (never cwd), and `await` its exported bootstrap. Measured on this Mac: `bun run entry.ts` where entry.ts does `await (await import('./daemon.ts')).bootstrap()` runs bootstrap in the SAME PID and leaves `import.meta.main === false` inside daemon.ts, while `bun run daemon.ts` directly leaves it TRUE and boots exactly once - both invocation paths work and neither double-boots. Same-PID matters: Type=notify plus WatchdogSec=90 (luna-server-install:262-264) need the daemon in the unit's main process, which a spawned child would not guarantee. This requires exporting the existing `bootstrap` (a `const` at chat-server.ts:4718, currently unexported): one `export` token, zero runtime change, and the `import.meta.main` guard stays exactly as-is. That explicit call is also why S08's shim rebuttal holds - the launcher never relies on a guard that a side-effect import turns off.\n\nRender both templates to `WorkingDirectory=${REPO_DIR}` (launchd: `--cwd $luna_dir`) plus `ExecStart=${bun_bin} run scripts/luna-chat-server-entry.ts`. `$bun_bin` is already ABSOLUTE in both templates, so bun resolution needs no PATH change, and the launchd plist additionally already carries `PATH=$(dirname \"$bun_bin\"):...` in EnvironmentVariables (launchd-plist.sh:64-65). The WorkingDirectory change is behaviorally inert on systemd hosts: luna-server-install:347 already writes `LUNA_REPO_ROOT=$REPO_DIR` into the EnvironmentFile, which shadows both cwd fallbacks in the daemon (chat-server.ts:2408 wakeWorkspacePath, :2471 defaultCwd). The launchd plist has NO LUNA_REPO_ROOT, so add it to EnvironmentVariables set to `$luna_dir` in this PR - otherwise the macOS wake workspace path silently moves from <luna_dir>/apps/ui-web to <luna_dir>.\n\nTRANSIENT PROBE, MARKED FOR DEATH. The launcher probes apps/server/src/chat-server.ts first, else apps/ui-web/scripts/chat-server.ts. That branch is needed only while some host's `previous` still resolves to a pre-S08 release. Put a `TODO(#issue):` marker directly above it naming the removal condition (every profile's `previous` resolves post-S08) and saying fix-or-remove, matching the repo's annotate-bad-code-in-place convention; S26 carries the removal step and asserts it.\n\nUNIT-RENDER ROLLBACK. Before writing either artifact, copy the installed one aside in the SAME directory with a `.prev` suffix (luna-chat-server.service.prev, com.user.luna-chat-server.plist.prev); neither supervisor loads those suffixes, so the backup is inert. Do the copy inside luna-server-install's write path so the guardian's auto-reconcile gets it too, not just a manual render. If the first start after a render fails, restore `.prev`, `systemctl daemon-reload`, `systemctl reset-failed`, restart (launchd: `launchctl bootout` then bootstrap the restored plist). That exact copy-pasteable sequence goes in the PR body. S02 lands first so the start limiter is already clearable.\n\nROLLBACK-TARGET GATE. A pre-S07 release contains no scripts/luna-chat-server-entry.ts, so the new unit cannot boot it, and do_rollback_releases flips to `$PREV` (luna-update-server:1723-1740) - precisely the release the installed unit must be able to start. Pair the render with the detector and gate it on the target: add unit_paths_current() and wire it at BOTH luna-guardian:524 and :670, because verified unit_hardened() (140-150) inspects ONLY Type and WatchdogUSec, reconcile_unit_if_idle early-returns on `unit_hardened && return 0` at 524, and its sole caller is gated `if ! unit_hardened` at 670 - editing only the 522-551 body, as both reviewed plans proposed, leaves the check permanently unreachable. unit_paths_current must report 'not current' ONLY when BOTH `current` and `previous` contain the launcher; while `previous` is still pre-S07 it reports current and deliberately leaves the old unit in place. Update the blocking assertion at test/releases-layout.test.ts:1355 (it pins both the old WorkingDirectory and the old ExecStart) and the launchd assertions in the same PR. S06a already retired the `run scripts/chat-server.ts` literals in rename-chat-server.test.ts, so test/deploy-scripts.test.ts is updated in the same PR (see below). AUDIT ROUND-2 ADDITIONS: (a) test/deploy-scripts.test.ts pins the OLD unit shape in five assertions that this slice's render breaks - :992 WorkingDirectory=.../apps/ui-web, :994 ExecStart=<bun> run scripts/chat-server.ts, :1377 ExecStart=/root/.bun/bin/bun run scripts/chat-server.ts, :2032 <string>.../apps/ui-web</string>, :2033 <string>scripts/chat-server.ts</string> - update all five to the new launcher shape in this PR (test:hostenv's fixed three-file list includes this file, so the slice gate itself goes red otherwise). (b) scripts/lib/port-guard.sh:37 identifies Luna's own daemon with the glob *chat-server.ts*, which does NOT match the new command line (it contains chat-server-entry.ts); ensure_port_free then treats Luna's own stale daemon as foreign and aborts install-mac.command at its :195 guard. Widen the predicate to *chat-server* while keeping the transitional *chat-server.ts* match, and update the explanatory comment at port-guard.sh:34. (c) QUALIFICATION of the bun-run empirical claim: the silent exit-0 no-READY behavior occurs ONLY when the bash-shebang file carries a .sh extension (Bun 1.3.14); extensionless or subdirectory paths exit 1 with a TS parse error. The static assertion (rendered ExecStart target ends in .ts and is not a shebang script) plus the no-READY behavioral assertion remains the required guard either way - state the qualified form in the PR body.",
      "acceptance": "Rendered systemd and launchd artifacts contain no app-specific path; `bun run scripts/luna-chat-server-entry.ts` boots the daemon from a pre-move tree AND from a post-move tree to a listening 4753 with /readyz green, in ONE process (daemon PID equals launcher PID); a negative test asserts the rendered ExecStart target ends in `.ts` and that file does not begin with a shebang, PLUS that a bash-shebang launcher fixture never emits READY - an exit-code-only assertion is explicitly insufficient because the broken shape can exit 0; the launchd plist carries LUNA_REPO_ROOT; `systemctl list-unit-files` shows zero `.prev` entries after a render; guardian fixtures cover four reconcile branches - idle renders exactly one `luna-server-install --units-only --no-enable --no-start`, active sessions >0 defers with a warning, `luna_active_ws_count` NON-ZERO EXIT defers with 'session count unknown' (luna-guardian:525-526), and a `previous` lacking the launcher leaves the old unit installed; the .prev restore sequence is exercised once on the dev profile with the transcript attached. port_guard_is_luna_cmd returns MATCH for a rendered-launcher command line (sourced-function test). The five deploy-scripts.test.ts unit-shape assertions are updated and bun run test:hostenv holds the S01 pass-count baseline within one.",
      "abandonCondition": "If unit_paths_current cannot compare rendered-vs-installed without duplicating render_service's template inside guardian, extract the two literals into scripts/lib/luna-deploy.sh first - two copies means the drift detector is itself the drift, which is the failure it exists to prevent. If the launcher cannot boot the daemon in the unit's MAIN PID (any design that requires spawning a child), STOP: Type=notify plus WatchdogSec=90 make main-PID identity load-bearing, and a watchdog-killed daemon on every host is worse than a version-dependent path. If exporting bootstrap changes any boot behavior at all, revert and report before rendering a single unit.",
      "verification": "grep -rn 'apps/ui-web\\|apps/server' scripts/luna-server-install scripts/lib/launchd-plist.sh\ngrep -n 'unit_paths_current' scripts/luna-guardian\ngrep -n 'TODO(#' scripts/luna-chat-server-entry.ts\ngrep -n 'LUNA_REPO_ROOT' scripts/lib/launchd-plist.sh scripts/luna-server-install\nbun run typecheck\nbun run test -- test/releases-layout.test.ts\nbun run test:hostenv\nshellcheck --severity=warning scripts/luna-server-install scripts/lib/launchd-plist.sh\nbash -c 'source scripts/lib/launchd-plist.sh; render_launchd_plist /usr/local/bin/bun /tmp/luna /tmp/lunahome' > /tmp/luna.plist && plutil -lint /tmp/luna.plist\nUI_WS_TOKEN=$(openssl rand -hex 24) bun run scripts/luna-chat-server-entry.ts & sleep 10; curl -fsS localhost:4753/readyz",
      "deployNote": "THE ONLY UNIT CHANGE IN THIS PLAN, and the last one ever needed. Sequenced after S02 so a bad render self-heals rather than locking out for 30 minutes. Roll out on the dev profile first, soak two guardian cycles, then stable. The guardian will not adopt the new unit until `previous` also carries the launcher, so expect adoption to lag one deploy per profile by design. Gate S08 on `readlink <deploy>/previous` resolving to a post-S07 release on every profile."
    },
    {
      "id": "S08",
      "title": "Move the daemon and its dependent ops CLIs to apps/server",
      "concern": "Relocate the 21-file server-runtime closure AND the seven ops CLIs whose relative imports resolve into it, with content edits limited to those forced import paths, one manifest pair, one tsconfig line and four transient dual-path sites.",
      "files": [
        "apps/server/package.json",
        "apps/server/src/",
        "apps/server/scripts/",
        "apps/ui-web/scripts/",
        "apps/ui-web/package.json",
        "package.json",
        "tsconfig.json",
        "bun.lock",
        "scripts/luna-update-server",
        "scripts/luna-server-install",
        "packages/core/src/doctor/doctor-enqueue.ts",
        "packages/core/src/doctor/doctor-enqueue.test.ts",
        "packages/core/src/jobs/job-ticker.ts",
        "packages/core/src/jobs/job-ticker.test.ts"
      ],
      "approach": "See the 2026-08-05 PLAN AMENDMENT changelog entry for the full discovery. Summary: `git mv` the verified 21-module closure (chat-server.ts, its 20 sibling modules, core-apps/*.html, co-located tests, plus example-mcp-ui-server.ts which turned out to be a test-only fixture of the closure's external-mcp-app-registry.test.ts) into apps/server/src/ - exactly three path segments deep, so the `../../..` repo-root arithmetic at dna-loader.ts:39 and system-loader.ts:50 needs ZERO edits (install-claude-skills.ts:52 and seed-probe-widgets.ts:198 carry the same arithmetic but those files do not move this slice, so they are untouched and still resolve). ALSO `git mv` the seven CLIs that import runtime-paths.ts/setup-login.ts by relative path (daily-brief-install, doctor-workflow-run, dream-wake-install + its -lib and __tests__ test, luna-doctor-workflow, push-through-install, seed-default-account, vault-migrate-keychain + its co-located test) into apps/server/scripts/, rewriting each CLI's own `./runtime-paths.js` or `./setup-login.js` import to `../src/<module>.js` - the only string-literal content edits inside moved files. NO SHIM for the daemon: chat-server.ts ends `if (import.meta.main) { void bootstrap() }`, so a side-effect import shim would leave that guard false and boot nothing; S07's launcher already calls the exported bootstrap explicitly instead. Hand-written artifacts: apps/server/package.json (name @luna/server, deps carried from apps/ui-web/package.json, matching only what the moved code actually imports) and the regenerated bun.lock, both mechanically required by CI HARD GATE 1 `bun install --frozen-lockfile`; one `apps/server/**` tsconfig exclude line (TODO naming S09, which gives apps/server its own tsconfig and removes it); root package.json's two vault:migrate-keychain script paths; apps/ui-web/package.json's server:chat script repointed at ../server/src/chat-server.ts (mechanically dead at its old path after the move). Four transient dual-path sites, each with a TODO(#440) marker mirroring scripts/luna-chat-server-entry.ts's pattern and retired at S26: scripts/luna-update-server's dream_wake_install_script() (used at both dry-run trace lines and the real post-deploy seed_dream_wake_jobs() call, so real dream/wake seeding does not silently degrade after this merge), scripts/luna-server-install's seed_default_account_script(), and scripts/luna-server-install's inline sd-notify version-skew guard at :459 (accepts both apps/server/src/sd-notify.ts and the ui-web fallback, else every post-S08 install would emit a false start-timeout warning). Classified but not moved (report only): dev-server.ts, mcp-demo.ts, mcp-cli.ts stay dev/ops-only demo tooling deferred to S10; smoke/*.smoke.ts stays - three of its nine files (belief-injection-boot, setup-mode-boot, survey-boot) import chat-server.js/credential-readiness.js and their imports go dangling, but S09's own text already calls these 9 files orphaned (wired to no script, no workflow, no tsc gate) and slated for S09 deletion, so this is accepted pre-existing-debt fallout, not a new gate regression.",
      "acceptance": "`git show --stat -M` reports the closure and the seven CLIs as renames; hand-written content is limited to apps/server/package.json, bun.lock, the seven CLIs' import-path edits, the tsconfig exclude line, the two root package.json script paths, apps/ui-web/package.json's server:chat pointer, and the four probe sites; the S07 launcher and a direct `bun run apps/server/src/chat-server.ts` both boot to a listening 4753 with /readyz green; a dry-run upgrade exercises the dream-wake-install probe; `bun run vault:migrate-keychain:dry-run` runs from its new path.",
      "abandonCondition": "If any moved file needs a content edit beyond the enumerated categories, or `bun run test:bun` (the luna.db contract, blocking from S06) goes red, or vitest silently drops moved test files from collection, stop and diagnose the coupling before re-attempting.",
      "verification": "git show --stat -M --diff-filter=R HEAD | tail -30\ngit diff HEAD~1 --stat -- test/ README.md\nbun install --frozen-lockfile\nbun run test && bun run test:bun\nUI_WS_TOKEN=$(openssl rand -hex 24) bun run scripts/luna-chat-server-entry.ts & sleep 10; curl -fsS localhost:4753/readyz\nbun run vault:migrate-keychain:dry-run 2>&1 | head -3\ngrep -n 'TODO(#' scripts/luna-update-server scripts/luna-server-install",
      "deployNote": "NO unit change and no shim - S07's launcher probes both entry paths. The four new probes are each rollback-safe by construction: a new engine over an old tree falls back to the old path, and an old engine never sees a new tree (the engine pin ships with the release that publishes it). Gated on S07 being accepted fleet-wide with `previous` resolving to a post-S07 release on every profile. Note the boot token must be >=16 chars or ui-ws-token.ts throws at import."
    },
    {
      "id": "S09",
      "title": "Typecheck apps/server and retire the orphaned smoke files",
      "concern": "The daemon boot path has never been typechecked; relocating it under apps/*/src makes that possible for the first time.",
      "files": [
        "tsconfig.json",
        "apps/server/tsconfig.json",
        "apps/server/package.json",
        "apps/server/src/chat-server.ts",
        "apps/server/src/core-apps.ts",
        "apps/server/src/external-mcp-app-registry.ts",
        "apps/ui-web/scripts/smoke/"
      ],
      "approach": "Remove the `apps/server/**` exclude added in S08 so the root include glob `apps/*/src/**/*.ts` picks the daemon up, retiring the plan's only bridge one PR after it was written. Add bun types via an apps/server/tsconfig.json declaring types:['bun'] to clear the module-resolution class first, then triage what remains: fix mechanical errors with conditional spreads and import dedupes, never with `any` or ts-ignore. Measure the error count first and record it as the PR's own baseline rather than trusting a number from planning. Then dispose of the 9 orphaned smoke/*.smoke.ts files (verified wired to no package.json script and no workflow): their own header says they exist because chat-server.ts had no tsc gate, a rationale this slice removes - so delete them, but check docs/HOW_TO_TEST_AND_VERIFY.md first, which documents one of them as a live procedure, and update that doc in the same PR.",
      "acceptance": "`bun run typecheck` includes apps/server/src and reports 0 errors there, with the pre-existing error count for the rest of the project unchanged; zero new `as any` / @ts-ignore in the diff.",
      "abandonCondition": "If fixing an exactOptionalPropertyTypes error requires changing a RUNTIME value rather than an annotation or a conditional spread, split that fix into its own slice. A behavior change smuggled inside a typecheck slice is exactly what breaks a cutover.",
      "verification": "bun run typecheck 2>&1 | tee /tmp/tc.txt; grep -c 'error TS' /tmp/tc.txt\ngrep -n 'apps/server' tsconfig.json\ngit diff HEAD~1 | grep -cE '@ts-ignore|@ts-expect-error|as any'\nbun run test\ngrep -rn 'smoke' package.json docs/HOW_TO_TEST_AND_VERIFY.md .github/workflows/ci.yml",
      "deployNote": "Type annotations and dead-file deletion only; no runtime behavior change reaches a host."
    },
    {
      "id": "S10",
      "title": "Relocate the remaining ops and dev-only CLIs to apps/server/scripts",
      "concern": "Six operator/dev CLIs still live under the app about to be deleted, having no relative-import coupling into the daemon closure S08 already relocated.",
      "files": [
        "apps/server/scripts/",
        "apps/ui-web/scripts/",
        "AGENTS.md",
        "PROJECT.md"
      ],
      "approach": "CORRECTED premise (see the S08 2026-08-05 PLAN AMENDMENT changelog entry): the original claim that all twelve tools here were 'verified as outside chat-server's import closure' was false for seven of them - daily-brief-install, doctor-workflow-run, dream-wake-install (+ -lib and its test), luna-doctor-workflow, push-through-install, seed-default-account and vault-migrate-keychain (+ its test) all import runtime-paths.ts or setup-login.ts by relative path, so S08 moved all seven already, rewrote their imports to `../src/<module>.js`, added the three luna-update-server/luna-server-install dual-path probes, and updated the two root package.json vault:migrate-keychain scripts. S08 also moved example-mcp-ui-server.ts (a test-only fixture of external-mcp-app-registry.test.ts, discovered mid-move) despite this slice's original list naming it a dev-only file to move here. What remains for THIS slice is the genuinely closure-free remainder: enable-wake, install-claude-skills, mcp-cli, seed-probe-widgets (all zero relative imports, confirmed), plus the dev-only dev-server.ts and mcp-demo.ts. `git mv` them into apps/server/scripts/, three segments deep, so install-claude-skills.ts:52 and seed-probe-widgets.ts:198's `../../..` repo-root arithmetic needs no edit. Outside the apps/*/src/** glob deliberately, so their pre-existing type errors do not regress the gate S09 made green - add a TODO(#issue) marker in each naming that debt. test/releases-layout.test.ts:505 needs NO edit: its fixture never materializes an apps/server/scripts/dream-wake-install.ts file (seeds only file.txt, bun.lock, scripts/lib/luna-deploy.sh), so S08's probe fallback already resolves to the literal path that assertion expects, verified by a real run (43/43 passed unchanged) - re-verify this still holds once this slice's own moves land. Update AGENTS.md:23 and PROJECT.md:11 in this PR rather than deferring path truth to the final slice; note AGENTS.md:23's `server:chat` command already works post-S08 (apps/ui-web/package.json's script was repointed there), so this is a documentation-accuracy edit, not a functional fix.",
      "acceptance": "apps/ui-web/scripts/ contains no .ts files; every relocated CLI runs from its new path via --help or --dry-run.",
      "abandonCondition": "If any relocated tool resolves the repo root by something other than three-levels-up (process.cwd(), git rev-parse), stop moving that file and report it - a cwd-dependent ops tool moved without adjusting its invocation is a silent deploy-time failure that surfaces at 3am.",
      "verification": "ls apps/ui-web/scripts/*.ts 2>&1\ngrep -rn 'apps/ui-web/scripts' scripts/ package.json test/ .github/ AGENTS.md PROJECT.md\nbun run test -- test/releases-layout.test.ts\nbun run test && bun run test:hostenv",
      "deployNote": "Deploy-TIME steps, not the boot path - a wrong path here fails a deploy step loudly rather than bricking a running daemon, which is why it is a separate cutover event from S07."
    },
    {
      "id": "S11",
      "title": "Remove the ui-web build and the dist completeness precondition from the deploy path",
      "concern": "The deploy engine builds and postcondition-checks a frontend that nothing serves, and gates release completeness on its artifact.",
      "files": [
        "scripts/luna-server-install",
        "scripts/luna-update-server",
        "package.json",
        "test/releases-layout.test.ts",
        "test/update-server.test.ts",
        "test/deploy-scripts.test.ts",
        "test/guardian.test.ts"
      ],
      "approach": "Verified SEVEN build/assert sites, one more than either reviewed plan enumerated: luna-server-install:386, and luna-update-server:610, 657, 1204, 1676 (the dry-run command-preview path, missed by both plans), plus the postconditions at 612-613 and 1207-1208 and the doc comments at 1595/1638. Critically also remove the dist clause from the release-completeness predicate: the function is `release_artifacts_ok()` at luna-update-server:562 - NOT `is_complete_release`, which does not exist in this repo and which both reviewed plans named - and its body at :564 is `[[ -f \"$rel/.complete\" && -d \"$rel/node_modules\" && -s \"$rel/apps/ui-web/dist/index.html\" ]]`. Drop the ui-web segment from root package.json:9 build and the `-p apps/ui-web/tsconfig.json` leg from :15 typecheck. Update the paired assertions in the SAME PR, including the blocking releases-layout fixtures that fabricate dist/index.html as a build-succeeded marker, and retire test/guardian.test.ts:160 (which asserts setup-mode does NOT contain the ui-web build) as vacuous rather than inverting it.",
      "acceptance": "A dry-run upgrade plan contains no @luna/ui-web build step and no dist/index.html precondition; a release directory with no apps/ui-web/dist is classified COMPLETE by release_artifacts_ok; `bun run build` and `bun run typecheck` exit 0.",
      "abandonCondition": "If dropping the dist clause leaves `.complete` + node_modules unable to distinguish a half-built release, add a positive assertion on a real server artifact instead of merely deleting the check - a weakened completeness predicate can flip a broken release into `current`.",
      "verification": "grep -rn \"filter .@luna/ui-web. build\\|ui-web/dist\\|apps/ui-web/tsconfig\" scripts/ test/ package.json\nsed -n '559,566p' scripts/luna-update-server\nbun run test -- test/releases-layout.test.ts\nbun run test:hostenv\nbun run build && bun run typecheck",
      "deployNote": "HARD ORDERING GATE: must land and reach the fleet's pinned engine BEFORE S12. The deploying engine is the PINNED copy of an older SHA, so a pre-S11 engine cannot materialize a post-S12 release - `release_artifacts_ok` would classify every new release incomplete and deploys would stop fleet-wide. Confirm each profile's engine pin has advanced past this SHA before merging S12."
    },
    {
      "id": "S12",
      "title": "Delete apps/ui-web, packages/design-system, and the two Stack 1 husks",
      "concern": "Remove the retired frontend tree and the packages only it consumed.",
      "files": [
        "apps/ui-web/",
        "packages/design-system/",
        "apps/ui-studio-tauri/",
        "packages/ui-shared-solid/",
        "scripts/lib/port-guard.sh",
        "bun.lock",
        "TODO.md"
      ],
      "approach": "Delete apps/ui-web entirely (by S10 it holds only frontend files), packages/design-system (verified its only consumer was apps/ui-web; Moon depends on @astryxdesign/core and @luna/ui-shared, not design-system, and has its own bridge at frontend-react/src/styles/astryx-moon-bridge.css), and the two Stack 1 husks I verified still on disk containing nothing but node_modules: apps/ui-studio-tauri/ and packages/ui-shared-solid/ (the latter missed by both reviewed plans despite being the identical class). Remove the now-dead `*apps/ui-web* dev*` process pattern at scripts/lib/port-guard.sh:38. Close TODO.md:31 (the watercolor-token duplication note), which this deletion resolves. Surface the design-system deletion as an explicit decision in the PR body - its watercolor tokens go with it - rather than burying it; if the Operator wants them for Moon's skin, salvage into astryx-moon-bridge.css in this same PR and diff against moon-palette.css to avoid a second palette source.",
      "acceptance": "`ls apps/` shows only agent-cli, server, ui-moon-tauri; `bun install --frozen-lockfile`, `bun run build`, `bun run typecheck` and `bun run test` all exit 0.",
      "abandonCondition": "If any consumer of @luna/design-system exists outside the deleted tree, land only the apps/ui-web deletion - deleting a package with a live consumer is unrecoverable without a revert.",
      "verification": "ls apps/ packages/\ngrep -rn '@luna/ui-web\\|@luna/design-system\\|apps/ui-web' . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=docs/superpowers --exclude-dir=docs/briefs --exclude-dir=docs/audits --exclude-dir=docs/discussions --exclude=bun.lock --exclude=CHANGELOG.md --exclude=NEXT.md  # PASS = zero hits in code and live docs; archival docs are exempt (same scoping as S26)\nbun install --frozen-lockfile && bun run build && bun run typecheck && bun run test",
      "deployNote": "Land in the same deploy window as S11, after confirming every profile's engine pin is post-S11."
    },
    {
      "id": "S13",
      "title": "Point install-mac.command at Moon instead of the Vite web UI",
      "concern": "macOS onboarding still guards port 5174 and boots a dev server for a frontend that no longer exists.",
      "files": [
        "install-mac.command",
        "scripts/lib/port-guard.sh",
        "README.md"
      ],
      "approach": "Rework the three touch points: the `ensure_port_free 5174 \"Vite Web UI\"` guard at install-mac.command:195, the background vite boot plus browser-open plus 'Web Chat URL' print at 271-298, and the duplicate flow at 421-425. Replace with: start the daemon via the S07 launcher, confirm /readyz, then direct the user to Moon. This is product copy a new user reads first, so propose the exact wording in the PR body for the Operator rather than deciding it in review.",
      "acceptance": "A clean run completes without referencing port 5174 or vite, ends with a healthy daemon and a clear next step naming Moon, and the one-time Claude login still completes.",
      "abandonCondition": "If the one-time Claude login genuinely needs a browser surface only ui-web provided, STOP and report it as a product gap before deleting the flow - an installer that cannot complete login is worse than a stale URL.",
      "verification": "grep -n '5174\\|Vite\\|ui-web' install-mac.command scripts/lib/port-guard.sh\nbash -n install-mac.command && shellcheck --severity=warning install-mac.command\nbun run test:hostenv -- test/deploy-scripts.test.ts",
      "deployNote": "New-user-visible. Attach a recorded terminal transcript of a full install run to the PR."
    },
    {
      "id": "S14",
      "title": "Moon chat: ChatCtx seam plus a frozen typed declaration for the sanitizer",
      "concern": "Establish the typed contract the chat rebuild mounts onto, with zero change to chat.html.",
      "files": [
        "apps/ui-moon-tauri/frontend-react/src/chat/chat-ctx.ts",
        "apps/ui-moon-tauri/frontend-react/src/chat/luna-markdown.d.ts",
        "apps/ui-moon-tauri/frontend-react/src/chat/luna-markdown.parity.test.ts"
      ],
      "approach": "Mirror the proven apps/ui-moon-tauri/frontend-react/src/panels/panel-ctx.ts PanelCtx shape (invoke/connectWs/hasTauri/win/label) as ChatCtx, backed by a direct ESM import of packages/ui-transport/src/browser.ts and packages/ui-ws/src/protocol.ts - not a fourth transport pattern. Declare window.LunaMarkdown in a .d.ts exposing exactly the five members the vendor IIFE attaches (verified at apps/ui-moon-tauri/frontend/vendor/moon-markdown.js:341: renderMarkdown, closeOpenFences, renderMarkdownStreaming, enhanceCodeBlocks, StreamRender). Add a byte-identity parity test modeled on the existing precedent packages/ui-shared/src/widget-sandbox.parity.test.ts, so the Operator's keep-the-sanitizer constraint is machine-checked rather than promised. moon-markdown.js is not touched, not ported, not wrapped in any transform.",
      "acceptance": "New modules typecheck and unit-test in isolation; chat.html has a zero-line diff in this PR; the parity test fails if any of the five members disappears.",
      "abandonCondition": "If the ESM ui-transport import cannot coexist with the vendor window.LunaTransport global still loaded by chat.html (two ConnectionManagers fighting over one socket), land only the ChatCtx type plus the markdown declaration and parity test, and defer the transport half to S18.",
      "verification": "bun run typecheck\nbun run test -- apps/ui-moon-tauri/frontend-react/src/chat/\ngit diff --stat HEAD~1 -- apps/ui-moon-tauri/frontend-react/chat.html\nbun run --filter '@luna/ui-moon-tauri' build:frontend",
      "deployNote": "Rides the Moon release feed (moon-v tag). Cannot brick a server. Every Moon slice must leave chat.html bootable, because the updater ships whatever is on the tag."
    },
    {
      "id": "S15",
      "title": "Moon chat: React message list and streaming render",
      "concern": "Replace the hand-written DOM renderer and its rAF loop with React, calling the audited sanitizer unchanged.",
      "files": [
        "apps/ui-moon-tauri/frontend-react/src/chat/chatModel.ts",
        "apps/ui-moon-tauri/frontend-react/src/chat/MessageList.tsx",
        "apps/ui-moon-tauri/frontend-react/src/chat/message-list.css",
        "apps/ui-moon-tauri/frontend-react/chat.html"
      ],
      "approach": "chat.html is 12005 lines with one inline script spanning 3068-11998. Convert the CHAT MODEL block (~9548-9875) into a pure typed reducer with no DOM access, and ChatRenderer (~9875-10457) plus the ChatLoop scheduler into a React component whose reconciler subsumes both. Markdown goes through `dangerouslySetInnerHTML={{__html: window.LunaMarkdown.renderMarkdown(text)}}` (renderMarkdownStreaming for in-flight segments), replicating the exact dispatch sites and passing the sanitizer's output straight through with no intermediate transform. CRITICAL scoping fix both reviewed plans got wrong: the `const renderMarkdown = LunaMarkdown.renderMarkdown` binding block (~8889-8893) is still read by ArtifactsEngine (~6143/6152, converted in S19) and by the window.__MoonInternals debug re-exports (~11950) that the repo's screenshot-verification workflow drives. Do NOT delete that block here; it dies in S19. Keep __MoonInternals intact. RE-ESTIMATION CHECKPOINT: this is where the Moon sequence gets re-costed against measured numbers rather than planning guesses. Baseline, measured: apps/ui-moon-tauri/test holds 39 .ts files totalling 15,602 lines, of which 28 reference panel.html or index.html. Record how many of those this slice actually had to touch, extrapolate S16-S20 from that ratio, and if the extrapolation blows the budget, re-plan the remaining Moon slices before opening S16 instead of pushing through.",
      "acceptance": "Streaming renders without flicker; scroll anchoring, hljs highlighting and the artifact preview path behave identically; moon-markdown.js byte-unchanged; a real-Tauri screenshot of a streamed turn with a fenced code block is attached; the Moon-suite re-estimation is recorded in the PR body against the measured baseline (39 test files, 15,602 lines, 28 coupled to panel.html/index.html).",
      "abandonCondition": "If ANY change to moon-markdown.js appears necessary, ABANDON - the sanitizer is a hard constraint and its output is trusted straight into innerHTML. If streaming regresses visibly, split into non-streaming turns then streaming rather than shipping a chat that flickers.",
      "verification": "git diff --stat HEAD~1 -- apps/ui-moon-tauri/frontend/vendor/moon-markdown.js\ngrep -n 'renderMarkdown' apps/ui-moon-tauri/frontend-react/src/chat/MessageList.tsx\nfind apps/ui-moon-tauri/test -name '*.ts' | xargs wc -l | tail -1\ngrep -rln 'panel.html\\|index.html' apps/ui-moon-tauri/test | wc -l\nbun run test -- apps/ui-moon-tauri\nbun run test -- apps/ui-moon-tauri/test/ws-contract.test.ts\nbun run --filter '@luna/ui-moon-tauri' build:frontend",
      "deployNote": "Highest-visibility UI change in the plan. Requires a real-Tauri screenshot, not a jsdom test, before merge."
    },
    {
      "id": "S16",
      "title": "Moon chat: composer, attachments and slash menu to React",
      "concern": "The input surface is still inline DOM bindings.",
      "files": [
        "apps/ui-moon-tauri/frontend-react/src/chat/Composer.tsx",
        "apps/ui-moon-tauri/frontend-react/src/chat/Attachments.tsx",
        "apps/ui-moon-tauri/frontend-react/src/chat/SlashMenu.tsx",
        "apps/ui-moon-tauri/frontend-react/chat.html"
      ],
      "approach": "Convert ComposerConfig (~7523-8014, the model/effort switcher), SmartBarEngine (~8015-8109), Attachments (~9226-9586) and SlashMenu (~11236-11595, which consumes the generated capabilities global). Effort types are TRIPLICATED across protocol, wire and reducer per prior repo history - widen all three or none; this slice widens none. Keep consuming the capabilities bundle for now; retiring it is a separate concern. Delete each converted vanilla block in this PR so the page never has two owners of the same DOM.",
      "acceptance": "Send, multiline input, paste-image, file attach, drag-drop, slash-command completion and the model/effort switcher all behave identically; no new protocol fields; screenshots of the composer with an attachment chip and an open slash menu attached.",
      "abandonCondition": "If attachments plus slash menu cannot both land under ~800 changed lines, split SlashMenu + SmartBarEngine into a follow-up rather than merging an unreviewable PR.",
      "verification": "bun run test -- apps/ui-moon-tauri\nbun run test -- apps/ui-moon-tauri/test/ws-contract.test.ts\nbun run --filter '@luna/ui-moon-tauri' build:frontend",
      "deployNote": "Screenshot-verify at both collapsed and expanded composer heights."
    },
    {
      "id": "S17",
      "title": "Moon chat: thread drawer to React, wrapping the drag state machine unchanged",
      "concern": "The thread drawer is the largest single inline subsystem and contains the most feel-critical interaction in Moon.",
      "files": [
        "apps/ui-moon-tauri/frontend-react/src/chat/ThreadDrawer.tsx",
        "apps/ui-moon-tauri/frontend-react/src/chat/useThreadDrag.ts",
        "apps/ui-moon-tauri/frontend-react/chat.html"
      ],
      "approach": "ThreadDrawerEngine spans roughly chat.html:4797-6018 (~1220 lines) plus ThreadCache and ThreadCreateState. Wrap apps/ui-moon-tauri/frontend/vendor/thread-drag-session.js (331 lines) in a useThreadDrag hook UNCHANGED - it is already a pure DOM-free Attached/Detached state machine modeled on Chrome's TabDragController, and every transition must still go through it; the hook owns only event plumbing and the ref handoff. Port the tuned resize constraints verbatim rather than substituting defaults. Preserve the drag-debug ring buffer and the installMoonE2E hook, which the screenshot workflow drives.",
      "acceptance": "List, pinning, open-in-window, resize snap behavior, drag-to-detach and re-dock all behave identically; thread-drag-session.js byte-unchanged; screenshots at collapsed, mid and max widths plus a recorded detach-and-redock.",
      "abandonCondition": "MUST SPLIT if the diff exceeds ~800 lines - natural seams are (a) list + cache + create-state, (b) pinned/session/empty/preview sections, (c) the drag hook. If drag feel regresses at all, hold for manual Operator exercise; a screenshot cannot prove detach threshold feel. Do not add a crisp ring/border/outline to .widget-shell while restyling the drawer edge - that is a standing hard rule.",
      "verification": "git diff --stat HEAD~1 -- apps/ui-moon-tauri/frontend/vendor/thread-drag-session.js\nbun run test -- apps/ui-moon-tauri\nbun run --filter '@luna/ui-moon-tauri' build:frontend",
      "deployNote": "Rides the Moon release feed. The drag hook is the one surface where Operator hands-on exercise substitutes for an automated gate, so hold the release until that happens."
    },
    {
      "id": "S18",
      "title": "Moon chat: connection lifecycle onto ESM transport, retiring the vendored bundle",
      "concern": "Chat's WebSocketEngine and PoolEngine still run on a generated vendor global, and the protocol version is triplicated.",
      "files": [
        "apps/ui-moon-tauri/frontend-react/src/chat/connection.ts",
        "apps/ui-moon-tauri/frontend-react/chat.html",
        "apps/ui-moon-tauri/frontend/vendor/ui-transport.js",
        "apps/ui-moon-tauri/scripts/bundle-ui-transport.ts",
        "apps/ui-moon-tauri/vite.config.ts",
        "package.json",
        "apps/ui-moon-tauri/test/ui-transport-staleness.test.ts"
      ],
      "approach": "Convert WebSocketEngine (~6365-6930) and PoolEngine (~6954-7464) onto the S14 ESM ConnectionManager, then delete vendor/ui-transport.js, scripts/bundle-ui-transport.ts, the root package.json:19 bundle:ui-transport script and the script tag. Import UI_WS_PROTOCOL_VERSION directly from packages/ui-ws/src/protocol.ts, removing one of the three hand-maintained copies. Two files both reviewed plans omitted and which their verification greps would have failed on: apps/ui-moon-tauri/vite.config.ts also references LunaTransport, and test/ui-transport-staleness.test.ts reads the deleted vendor file off disk - retire or repoint both in this PR.\n\npool-engine.js DISPOSITION, which the audited draft left orphaned and mis-stated: it is KEPT, not deleted, and it is a DIFFERENT file from the one this slice deletes. Verified apps/ui-moon-tauri/frontend-react/index.html:41 - the Moon hub surface - loads /vendor/pool-engine.js, and frontend-react/src/main-index.tsx:8-16 documents that hub vendor set (moon-appearance, moon-protocol, moon-ws, moon-session, pool-engine, moon-hub) as deliberately untouched, with PoolEngineHelper among the globals hubEngines.ts's HubController reads. apps/ui-moon-tauri/test/pool-engine.test.ts and test/moon-hub.test.ts:36 read that file off disk and BOTH STAY UNCHANGED - the audited draft's claim that pool-engine.test.ts reads the deleted vendor file is wrong, and acting on it would have deleted live hub coverage. This slice removes only chat.html's own `<script src=\"/vendor/pool-engine.js\">` tag and the inline PoolEngine that delegated to it. test/chat-window.test.ts and test/thread-drawer-pool-edge-cases.test.ts also loadVendorInto pool-engine.js; those are chat fixtures and are retired in S20, not here.\n\nDo NOT touch vendor/moon-ws.js: the hub and panels still use it and hubEngines.ts documents that separation as deliberate. This reduces three transport patterns to two; collapsing the last one is a separate concern.",
      "acceptance": "Chat connects, reconnects after a real server restart, and parallel-panel pooling still works; zero remaining LunaTransport references; chat.html drops roughly 2600 lines; vendor/pool-engine.js is byte-unchanged and both hub tests still pass.",
      "abandonCondition": "If PoolEngine's per-connection multi-registration semantics cannot be reproduced through ConnectionManager without a server-side change, land only the WebSocketEngine half - that server-side gap is a known separate follow-up and must not be smuggled into a frontend PR, and shipping a chat that drops parallel panels is a user-visible regression.",
      "verification": "grep -rn 'LunaTransport\\|ui-transport.js\\|bundle:ui-transport' apps/ui-moon-tauri package.json\ngrep -rn 'pool-engine' apps/ui-moon-tauri --include='*.html'\ngit diff --stat HEAD~1 -- apps/ui-moon-tauri/frontend/vendor/pool-engine.js\nbun run test -- apps/ui-moon-tauri/test/pool-engine.test.ts apps/ui-moon-tauri/test/moon-hub.test.ts\nbun run test -- apps/ui-moon-tauri/test/ws-contract.test.ts\nbun run test -- apps/ui-moon-tauri\nbun run --filter '@luna/ui-moon-tauri' build:frontend",
      "deployNote": "Exercise a real server restart against a running Moon to prove reconnect, not just a fresh connect."
    },
    {
      "id": "S19",
      "title": "Moon chat: remaining engines to React",
      "concern": "Convert the last vanilla engines so the inline script can be deleted.",
      "files": [
        "apps/ui-moon-tauri/frontend-react/src/chat/overlays/",
        "apps/ui-moon-tauri/frontend-react/chat.html"
      ],
      "approach": "Convert the remainder of the inline script: ArtifactsEngine (~6019-6320, including the two LunaMarkdown call sites and the binding block S15 deliberately preserved - delete it HERE, once its last reader is gone), SecretPromptEngine, SurveyEngine, SuggestedActionsEngine, FeedbackEngine, ResultToasts, UpdateBanner, MoonFrames, VoiceEngine, LocalShell, MoonFace/MoonBar. SecretPromptEngine must keep the property that secrets never enter the transcript; FeedbackEngine keeps its screenshot crop budget. Keep window.__MoonInternals alive until S20.",
      "acceptance": "All five artifact kinds preview, tool-call expand/collapse works, secret entry stays out of the transcript, survey and suggested-action chips round-trip, and crosshair feedback still mints an fbj- job; a screenshot per converted surface.",
      "abandonCondition": "MUST SPLIT into at least four PRs grouped by surface (artifacts; secret+survey+suggested-actions; feedback+toasts+banner; frames+voice+local-shell+bar). Roughly 4000 lines remain here; do not attempt one slice. If the secure secret-entry path cannot be proven to keep secrets out of the transcript under React state, that half stops - the property is non-negotiable.",
      "verification": "bun run test -- apps/ui-moon-tauri\ngrep -n 'ArtifactsEngine\\|SecretPromptEngine\\|FeedbackEngine' apps/ui-moon-tauri/frontend-react/chat.html\nbun run --filter '@luna/ui-moon-tauri' build:frontend",
      "deployNote": "Rides the Moon release feed across at least four PRs; each must leave chat.html bootable because the updater ships whatever is on the tag."
    },
    {
      "id": "S20",
      "title": "Delete the vanilla chat layer, the dual-mount bridge and the legacy fixtures",
      "concern": "Remove the superseded vanilla shells and the tests that read them off disk, and settle the disposition of every vendor script chat.html loads.",
      "files": [
        "apps/ui-moon-tauri/frontend-react/chat.html",
        "apps/ui-moon-tauri/frontend-react/src/chat/chat-chrome-mount.tsx",
        "apps/ui-moon-tauri/frontend-react/src/chat/vendor/thread-drag-session.js",
        "apps/ui-moon-tauri/frontend/vendor/capabilities.js",
        "apps/ui-moon-tauri/scripts/bundle-capabilities.ts",
        "apps/ui-moon-tauri/test/capabilities-staleness.test.ts",
        "package.json",
        "apps/ui-moon-tauri/frontend/index.html",
        "apps/ui-moon-tauri/frontend/panel.html",
        "apps/ui-moon-tauri/frontend/panels/",
        "apps/ui-moon-tauri/test/"
      ],
      "approach": "Reduce chat.html to a shell: the bundled React entry plus the frozen globals, with the orphaned CSS deleted per-selector against a grep proving zero consumers. Fold chat-chrome-mount.tsx's ad-hoc mounts into the single chat root. Delete frontend/index.html and frontend/panel.html (verified unreachable: tauri.conf.json frontendDist points at ../frontend-react/dist) and frontend/panels/.\n\nVENDOR-LAYER MANIFEST. The charter says 'delete the vanilla vendor layer' for CHAT, and this is where that is settled for ALL THIRTEEN scripts chat.html loads, each disposition measured with `grep -rln 'vendor/<file>' apps/ui-moon-tauri/frontend-react/*.html apps/ui-moon-tauri/frontend/*.html`. KEPT-AS-FROZEN-DEP, still a script tag in chat.html and byte-identical: moon-markdown.js:16, the audited sanitizer and an Operator hard constraint (also loaded by frontend-react/widget.html), and highlight.min.js:10, third-party vendored highlight.js which moon-markdown.js:214-241 reads as window.hljs inside enhanceCodeBlocks, so it cannot become an ESM import without editing the frozen sanitizer (also loaded by widget.html). KEPT-NON-CHAT, chat.html drops its tag and the file stays for a named surface: moon-protocol.js:14, moon-ws.js:15, moon-appearance.js:36 and moon-session.js:56 serve frontend-react/index.html + panel.html + widget.html; moon-dock.js:17, moon-resize.js:18 and moon-window-float.js:38 serve frontend-react/panel.html + widget.html; pool-engine.js:51 serves frontend-react/index.html:41, the hub, plus test/pool-engine.test.ts and test/moon-hub.test.ts:36. REPLACED-BY-REACT, file deleted: ui-transport.js:44 went in S18 with its generator and root script, and capabilities.js:50 goes here - verified chat.html is its ONLY html consumer and it is GENERATED from packages/capabilities/src/index.ts by apps/ui-moon-tauri/scripts/bundle-capabilities.ts, so the React SlashMenu imports that package directly and this slice deletes the generated bundle, the generator, root package.json:20 `bundle:capabilities` and test/capabilities-staleness.test.ts. thread-drag-session.js:20 is chat-only and byte-frozen: `git mv` it into the bundle at frontend-react/src/chat/vendor/thread-drag-session.js with ZERO content change (provable with `git show --stat -M`) and side-effect-import it, so both the vanilla vendor copy and its script tag disappear while S17's byte-identity guarantee survives the move. END STATE: chat.html loads the bundled React app plus exactly two frozen script tags, highlight.min.js and moon-markdown.js, and nothing else.\n\nSCOPE CORRECTION, MEASURED, replacing the audited draft's 17 and one reviewed plan's 10: `grep -rln 'panel.html\\|index.html' apps/ui-moon-tauri/test` returns 28 files (identical for the escaped and unescaped patterns), and the suite is 39 .ts files totalling 15,602 lines. Beyond moon-app / update-pip / panel-launcher / panel-connection / moon-native-titlebar / panel-actions / panel-briefing / panel-flow / panel-route-binding / panel-window / panel-workflows / settings-connectors-panel it includes settings-models-panel, settings-vault-panel, settings-apps-panel, settings-updates-panel, settings-connection-panel, settings-appearance-panel, settings-launcher-panel, panel-skills, panel-general, panel-agents, panel-now, panel-voice, panel-registry-conformance, chat-window, window-float and react-scaffold. Enumerate by RUNNING the grep, then port each assertion that guards a live invariant onto the React tree and delete the rest with greps.\n\nCRITICAL: src-tauri/src/windows.rs:57 is `include_str!(\"../../frontend/vendor/widget-registry.json\")`, a compile-time Rust path dependency - that file must stay at exactly that path or windows.rs moves in lockstep, and `cargo build` is the only signal.",
      "acceptance": "chat.html contains no inline application script and exactly two vendor script tags (`grep -c '<script src=\"/vendor/' chat.html` equals 2); the thirteen-file disposition manifest is reproduced in the PR body with its grep output, one justification line each; the Tauri app builds and boots as a bundled typed React app; moon-markdown.js, highlight.min.js, widget-sandbox.js and thread-drag-session.js are all byte-identical under `git show --stat -M`; `bun run bundle:capabilities` no longer exists and nothing references vendor/capabilities.js; every one of the 28 legacy fixtures is ported or deleted with a grep; full manual chat exercise in a real Tauri window.",
      "abandonCondition": "If any legacy fixture is the sole coverage of a live invariant with no React equivalent, port it first in its own PR and only then delete the fixture. If thread-drag-session.js cannot move without a content edit, leave it where it is and keep its script tag rather than editing it - byte identity outranks the zero-vendor-tags goal, and the manifest records the exception.",
      "verification": "grep -c '<script src=\"/vendor/' apps/ui-moon-tauri/frontend-react/chat.html\ngrep -rln 'panel.html\\|index.html' apps/ui-moon-tauri/test | wc -l\ngrep -rn 'capabilities.js\\|bundle:capabilities' apps/ui-moon-tauri package.json\ngrep -rln 'frontend/index.html\\|frontend/panel.html\\|panels/settings' apps/ui-moon-tauri\nls apps/ui-moon-tauri/frontend/vendor/widget-registry.json\ngit show --stat -M HEAD | grep -E 'thread-drag-session|moon-markdown|highlight.min|widget-sandbox'\ncargo build --manifest-path apps/ui-moon-tauri/src-tauri/Cargo.toml\nbun run test && bun run test -- apps/ui-moon-tauri/test/ws-contract.test.ts",
      "deployNote": "Cut a Moon release. WKWebView caches aggressively; the version bump triggers the existing purge in lifecycle.rs, so the version files must be in lockstep (CI HARD GATE 3 enforces the triple)."
    },
    {
      "id": "S21",
      "title": "Scaffold the deploy binary and teach the engine pin to publish it",
      "concern": "Stand up the compiled artifact and its publish path before any logic is ported into it.",
      "files": [
        "apps/deploy-cli/",
        "scripts/luna-guardian",
        "test/guardian.test.ts",
        "docs/deploy-binary.md"
      ],
      "approach": "Create apps/deploy-cli as a workspace app (under apps/*/src so it is typechecked from birth) compiling to a single-file binary, implementing only --version, --help, subcommand dispatch stubs matching the bash argv surfaces, and the exit-code constants documented at luna-update-server:171-184. Every subcommand exits 2 with 'not implemented'. Teach publish_engine to ship it. TWO verified constraints neither reviewed plan handled correctly. (1) BOOTSTRAP: publish_engine (luna-guardian:1001-1040) copies six NAMED bash scripts plus lib/*.sh out of the deployed release, so the engine that knows how to publish a binary must itself ship AS BASH through the pin and be accepted first - nothing can run the binary until a pin containing it is current, and no pin contains it until this bash change is current. (2) GLOB COLLISION: line 1026 is `chmod +x \"$tmp\"/luna-*; bash -n \"$tmp\"/luna-* \"$tmp/lib/\"*.sh`, so an artifact named `luna-deploy` matches the glob for BOTH the chmod and the syntax check and fails `bash -n`. Name the artifact `deploy-cli`, off the luna-* prefix, and chmod it on an explicit line. Also state WHO builds it and for which platform: the deploy targets are Linux containers while the dev machine is arm64 macOS, so the binary is built ON THE HOST inside publish_engine (bun is already a hard dependency there) rather than cross-compiled from CI or committed to a public repo. Record in docs/deploy-binary.md the architectural decision this locks in - the binary owns the state machine and ServerUpdateDriver gains NO new methods - and get the Operator's explicit sign-off in this PR, because the charter text does not disambiguate it.",
      "acceptance": "A publish run produces a pin containing the six bash scripts, lib/*.sh, an executable deploy-cli and .complete; `bash -n` still runs over exactly the bash files; a pin published by an OLDER engine and missing the binary is still classified complete.",
      "abandonCondition": "If the Operator prefers extending ServerUpdateDriver with guardian-shaped methods (timer supervision, debounce journal, repair ladder), STOP the entire fold and redesign - everything from S22 on would be built on the wrong foundation. If the binary cannot be built on the host, do not substitute a cross-compiled artifact you cannot verify.",
      "verification": "cd apps/deploy-cli && bun build --compile --outfile=/tmp/deploy-cli src/main.ts && /tmp/deploy-cli --version\nbun run test -- apps/deploy-cli\nbun run test:hostenv -- test/guardian.test.ts\ngit diff HEAD~1 -- packages/server-registry/src/driver/contract.ts\nsed -n '1018,1030p' scripts/luna-guardian",
      "deployNote": "Ships as bash through the engine pin and must be ACCEPTED before anything invokes the binary. Out of order here means an unpublishable engine and manual recovery on a live host."
    },
    {
      "id": "S22",
      "title": "Binary: update, journal, session guard, readiness and rollback with golden parity",
      "concern": "Port the update engine's crash-safety and rollback primitives with byte-level proof against the bash they replace.",
      "files": [
        "apps/deploy-cli/src/update/",
        "apps/deploy-cli/test/update/",
        "packages/server-registry/src/driver/luna-chat-server.ts",
        "scripts/luna-autodeploy"
      ],
      "approach": "Port the transaction journal (luna-update-server:993-1042, prepared/applied/restarting/verifying with next-tick resumption), the fail-closed session guard with its logged --operator-override (1446-1491), the stop/settle/start restart primitive (1260-1372, whose 6s settle exists specifically to avoid SQLITE_CANTOPEN from unflushed WAL handles - that rationale must survive in a comment), the bounded readiness poll (1052-1130), fail_forward/do_rollback (1780-1848), and the layout flip using fs.rename, which needs no `-T` equivalent and permanently retires the GNU-mv dependency S01 worked around. Preserve the on-disk key=value text formats of transaction/status/health files EXACTLY - they are a de facto contract shared with scripts/luna-guardian-remote-check's sed parsing, and preserving them is what lets binary and bash hosts coexist without a flag day. Preserve the literal `ROLLED BACK to` stderr marker: packages/server-registry/src/driver/luna-chat-server.ts classifies outcomes on exit code PLUS that string, so drift silently reports a successful auto-rollback as a hard failure. Gate selection behind LUNA_DEPLOY_ENGINE=binary|bash in luna-autodeploy, DEFAULT BASH. Build the golden parity harness here: run both implementations on the same fixture and diff exit codes, stderr markers and on-disk state bytes, including crash-injection between phases and BIDIRECTIONAL interop (a layout produced by the binary is rolled back by bash and vice versa).",
      "acceptance": "Parity is byte-identical across success, readiness-failure-with-rollback, session-guard defer, lock contention and every crash-resume phase; bidirectional interop passes; with LUNA_DEPLOY_ENGINE unset behavior is bit-for-bit unchanged.",
      "abandonCondition": "If any parity scenario diverges in exit code or in the `ROLLED BACK to` marker, keep bash default and stop. If the session guard can be made to proceed with live sessions under ANY binary code path, revert to bash-only for restart - fail-closed is the invariant. If bidirectional interop fails, the escape hatch is fiction and the fold stops.",
      "verification": "bun run test -- apps/deploy-cli/test/update/\nbun run test -- packages/server-registry\ngrep -rn 'ROLLED BACK to' apps/deploy-cli/src scripts/luna-update-server packages/server-registry/src\nbun run test:hostenv\nscripts/luna-guardian-remote-check <profile>   # must parse a binary-written status file unchanged",
      "deployNote": "MUST SPLIT into at least three PRs on contact (journal+state IO; restart+session guard; readiness+rollback+layout). Publishes through the pin but changes no default, so it is safe to deploy fleet-wide before anyone opts in."
    },
    {
      "id": "S23",
      "title": "Flip the deploy engine default to the binary",
      "concern": "Make the binary the default after an accept-grade gate proves it on live hosts.",
      "files": [
        "scripts/luna-autodeploy",
        "packages/server-registry/src/driver/luna-chat-server.ts",
        "docs/deploy-binary.md"
      ],
      "approach": "Change the LUNA_DEPLOY_ENGINE default from bash to binary, and point LunaChatServerDriver's pinnedScriptPath default at the binary - the contract needs no change, it already tolerates an opaque CLI, and this wires packages/server-registry into production for the first time. Gate the merge on a per-profile accept run with the binary already published and exercised for at least one real deploy per profile, and check the promotion window is actually viable first: guardian_status_evidence gates freshness against LUNA_GUARDIAN_HEALTH_WINDOW_SEC (default 900) while stable's timer interval is also 15min, and the code's own comment warns a cadence at or beyond the window reads every heartbeat as stale and starves auto-promotion. Measure and tune that before relying on it. Keep the one-line-revert property.",
      "acceptance": "Every profile completes at least one full deploy through the binary and passes accept with min-cycles 2; setting LUNA_DEPLOY_ENGINE=bash restores the bash path with no other change; promotion freshness is measured, not assumed.",
      "abandonCondition": "If any profile's accept run fails, or a binary deploy produces a rollback the bash engine would not have, REVERT the default immediately. Keep it a one-line revert by design.",
      "verification": "grep -n 'LUNA_DEPLOY_ENGINE' scripts/luna-autodeploy\ngrep -n 'pinnedScriptPath' packages/server-registry/src/driver/luna-chat-server.ts\nbun run test -- packages/server-registry\nbun run test:hostenv",
      "deployNote": "The highest-consequence single line in the plan: it flips which code path performs every future production restart on every host. Dev profile first, soak, Operator sign-off before stable. No hostnames in the PR body - the repo is public."
    },
    {
      "id": "S24",
      "title": "Fold autodeploy and guardian into the binary",
      "concern": "Move the pull-trigger wrapper and guardian's supervision state machine into the binary.",
      "files": [
        "apps/deploy-cli/src/autodeploy/",
        "apps/deploy-cli/src/guardian/",
        "apps/deploy-cli/src/registry/",
        "scripts/luna-autodeploy",
        "scripts/luna-guardian",
        "scripts/luna-doctor",
        "scripts/lib/luna-registry.sh",
        "test/guardian.test.ts",
        "test/deploy-scripts.test.ts"
      ],
      "approach": "Port do_deploy / do_repair's two-rung ladder / do_validate, then guardian's debounced health journal (250-343), status heartbeat (192-235), unit_hardened plus S07's unit_paths_current, reconcile_unit_if_idle, the accept-grade promotion gate, self-rendered units with the converged-diff reload contract, and publish/prune (keep=5). Drop the legacy install_timer/uninstall_timer path - guardian's own timer supersedes it and the migration marker already exists to sunset it; folding a deprecated path into a new binary is exactly the speculative work to avoid. Port redact() (luna-guardian:30-35) VERBATIM in behavior including the chmod 600 on diagnose captures - it is the guardian-side complement to the CI 40-hex secret scan. Keep the SINGLE-EVIDENCE-PATH invariant the code calls out in-line: human accept and automatic promotion must funnel through ONE evidence function, asserted by a test. Preserve the rc contract guardian's update_then_refresh branches on (0 repaired, 1 rolled back, 3 defer, 4 lock contention).\n\nREGISTRY: HARD PRECONDITION, not a risk note. Port the registry TOML reader as a library inside the binary and delete scripts/lib/luna-registry.sh only once its last bash caller is gone - do NOT add a shell-out wrapper that would be rewritten and then orphaned. Its live consumers are scripts/luna-guardian, scripts/luna-autodeploy AND scripts/luna-doctor, and luna-doctor is retained in bash through S25. This slice does not start until luna-doctor's resolution is decided and recorded here, one of exactly two ways: repoint luna-doctor at the binary's registry subcommand, or fold luna-doctor too. Deferring the choice makes S25's 'one parser' outcome unachievable and turns a deletion PR into an unplanned rewrite.",
      "acceptance": "Parity green for the debounce sequence, both ladder rungs, converged-diff unit reload, and prune retaining exactly 5 with every live pin protected; accept and auto-promotion demonstrably share one code path; redact() unit tests cover every pattern the bash version handles; luna-guardian-remote-check parses the binary's status file unmodified; luna-doctor reads the registry through the binary (or is folded), and `grep -rln 'luna-registry.sh' scripts/` returns only files this stack still deletes.",
      "abandonCondition": "If ANY redaction pattern cannot be reproduced with certainty, STOP the entire guardian fold - a diagnose capture leaking a token to disk on a public repo is worse than 1314 lines of bash. If accept and promotion end up with two evidence paths, rework. If prune's live-pin precondition cannot be reproduced with equal strictness, keep publish/prune in bash and shell out - deleting the wrong engine pin is unrecoverable. If luna-doctor cannot be repointed without changing its output contract, STOP and report BEFORE porting the registry reader: shipping two registry parsers is the exact duplication this fold exists to remove.",
      "verification": "bun run test -- apps/deploy-cli/test/guardian/ apps/deploy-cli/test/autodeploy/\nbun run test:hostenv -- test/guardian.test.ts test/deploy-scripts.test.ts\ngrep -rln 'luna-registry.sh' scripts/\ngrep -rnE '[0-9a-f]{40,}' apps/deploy-cli/src apps/deploy-cli/test\ndiff <(bash scripts/luna-guardian check 2>&1) <(/tmp/deploy-cli guardian check 2>&1)  # parity on a fixture profile; unit-render parity goes through the 'luna-server-install --units-only' path guardian actually shells out to (guardian has no 'render' subcommand)",
      "deployNote": "MUST SPLIT into at least four PRs (autodeploy; journal+status; ladder+reconcile; accept+units+publish/prune), each accepted through the pin before the next lands. Guardian runs as root against live units - non-production profile only, several full timer cycles, Operator sign-off before stable. Never squash-merge or rewrite history a deployed pin's SHA depends on."
    },
    {
      "id": "S25",
      "title": "Delete the bash deploy engine; the engine pin is the escape hatch (Operator-approved)",
      "concern": "Collect the fold's deletion payoff instead of leaving two engines and a parity harness forever, without silently dropping the escape hatch the charter promised.",
      "files": [
        "scripts/luna-update-server",
        "scripts/luna-autodeploy",
        "scripts/luna-guardian",
        "scripts/lib/luna-registry.sh",
        "scripts/lib/luna-deploy.sh",
        "apps/deploy-cli/test/",
        "docs/ops/deploy-recovery.md (NEW)",
        "docs/deploy-binary.md",
        "NEXT.md"
      ],
      "approach": "OPERATOR ADJUDICATION (Mr. Cobb, 2026-08-03): the full-bash-deletion variant is APPROVED - no scripts/luna-recover is created. Delete the bash engine the binary replaced (luna-update-server 2073 + luna-guardian 1314 + luna-autodeploy 836 + luna-registry.sh 513 = ~4736 LOC), retire the LUNA_DEPLOY_ENGINE selector, and retire the cross-engine parity harness - migration scaffolding, not a maintenance obligation. PRECONDITION unchanged from S24: luna-registry.sh is deleted only once its last bash caller is gone (luna-doctor resolution per S24's hard precondition). THE ESCAPE HATCH IS THE ENGINE PIN: publish_engine's content-addressed pins with .complete markers, five prior self-contained engines retained per host by prune (keep=5). Write docs/ops/deploy-recovery.md (NEW) with two copy-pasteable manual procedures, each with the exact commands: (a) ENGINE-PIN ROLLBACK - flip the engine `current` symlink to a prior pin and re-run the update; recovers from a broken NEW engine artifact. (b) BARE-HANDS RELEASE ROLLBACK - stop the unit, flip the release `current` symlink to the `previous` target, start the unit; four commands, zero scripts, recovers a host when NO engine will run. Both procedures are EXERCISED once on a non-production profile and the transcripts recorded in the PR body (TEST-003: verify by living it). NEXT.md's Stack 3 charter line ('keeping a bash escape hatch') is amended in this slice to record the adjudication with its date, mirroring the dream+wake rejection entry style. docs/deploy-binary.md's pending-sign-off paragraph is resolved to point at the recorded decision.",
      "acceptance": "The four bash engine files are gone and `grep -rn 'LUNA_DEPLOY_ENGINE' scripts/ apps/ packages/` returns zero hits; docs/ops/deploy-recovery.md documents both manual procedures with exact commands; both procedures were exercised on a non-production profile with transcripts in the PR body; only one deploy engine and one registry parser remain in the repo; NEXT.md records the Operator adjudication dated 2026-08-03.",
      "abandonCondition": "If EITHER manual procedure (engine-pin rollback, bare-hands release rollback) cannot be demonstrated end to end on a live non-production profile, DO NOT DELETE - hold the bash in place and report to the Operator. An escape hatch that has never been exercised is not an escape hatch. If the documented procedures start needing conditionals or per-host variance to work, the recovery story is not simple enough to be script-free - stop and report rather than reintroducing a script unilaterally.",
      "verification": "ls scripts/luna-update-server scripts/luna-autodeploy scripts/luna-guardian scripts/lib/luna-registry.sh 2>&1  # PASS = No such file for all four\ngrep -rn 'LUNA_DEPLOY_ENGINE' scripts/ apps/ packages/  # PASS = zero hits\ngrep -n 'escape hatch' NEXT.md  # PASS = adjudication entry dated 2026-08-03\nls docs/ops/deploy-recovery.md\nwc -l scripts/luna-* scripts/lib/*.sh 2>/dev/null\nbun run test:hostenv\nbun run test -- apps/deploy-cli",
      "deployNote": "Only after S24 has held healthy across several full timer cycles on every profile. Record BOTH exercised procedures - the engine-pin rollback and the bare-hands release rollback - as transcripts in the PR body."
    },
    {
      "id": "S26",
      "title": "Full-agent E2E verification, gate promotion, probe retirement and docs truth-up",
      "concern": "Prove the reshaped system end to end against real state, retire the last transient migration branch, and retire the docs describing the deleted architecture.",
      "files": [
        "scripts/luna-chat-server-entry.ts",
        "scripts/luna-update-server",
        "scripts/luna-server-install",
        "test/",
        "NEXT.md",
        "DESIGN.md",
        "README.md",
        "AGENTS.md",
        "PROJECT.md",
        "TESTING.md",
        "docs/HOW_TO_TEST_AND_VERIFY.md",
        "docs/next/",
        ".github/workflows/ci.yml"
      ],
      "approach": "Re-run the S06 luna.db 3-step gate against fresh COPIES and compare retrieval recall to the baseline S06 recorded. Then a real end-to-end pass, not a suite: launch Moon against a freshly deployed apps/server daemon and hold a multi-turn conversation exercising streaming, a tool call, all artifact kinds, memory write and recall, job creation with the result delivered into the thread, thread drawer drag-out, and slash commands - with screenshots. Drive one full update through the binary including a forced rollback. Promote `bun run typecheck` from continue-on-error to blocking, since S09 made apps/server green and the whole project is at 0.\n\nRETIRE ALL FOUR TRANSIENT PROBES carrying a TODO(#440) marker, as the Operator's simplicity constraint demands: verify per profile that `readlink <deploy>/previous` resolves to a post-S08 release, record that output as the evidence, then delete every apps/ui-web fallback branch - scripts/luna-chat-server-entry.ts's own probe, scripts/luna-update-server's dream_wake_install_script(), and scripts/luna-server-install's seed_default_account_script() AND its inline sd-notify version-skew guard - so each site names exactly one path and the migration leaves no permanent branching behind. Do it before the docs truth-up so the docs describe the final shape.\n\nTruth up the live docs only. SCOPE CORRECTION on both reviewed plans' closing grep: `grep -rln 'apps/ui-web' docs/ *.md` returns ~35 files, roughly 20 of which are archival records under docs/superpowers/plans, docs/briefs, docs/audits and docs/discussions that correctly describe what was true when written. Scope the assertion to live docs (README, DESIGN, AGENTS, PROJECT, TESTING, NEXT, docs/HOW_TO_TEST_AND_VERIFY.md, docs/next/) and say so explicitly rather than mass-rewriting history to satisfy a gate.",
      "acceptance": "All three cutover-gate steps pass against copies with recall within the abandon band; the full Moon agent turn completes with screenshots and thread transcript attached; the binary update-plus-forced-rollback trace is attached; scripts/luna-chat-server-entry.ts, scripts/luna-update-server and scripts/luna-server-install each name exactly one path with zero TODO(#440) markers remaining, with the per-profile `previous` readlink output recorded as the evidence that retiring every branch was safe; typecheck and test:bun are both blocking and green; no LIVE doc references a deleted path.",
      "abandonCondition": "If retrieval recall regresses more than 5% against the S06 baseline, or the cutover gate shows any non-no-op migration, DO NOT close the stack and do not ship the docs cleanup - open the regression as its own slice and bisect. A green docs PR over a regressed metric is exactly what NEXT.md decision 5 exists to prevent. If any profile's `previous` still resolves to a pre-S08 release, do NOT delete the probe branch - leave the TODO marker in place and close the stack with that one item open rather than stranding a host that cannot roll back. If a newly-blocking gate is not reproducibly green across three consecutive runs, revert it to non-blocking with an issue rather than leaving CI red.",
      "verification": "bun run test && bun run test:bun && bun run test:hostenv && bun run typecheck && bun run build\ngrep -rn 'TODO(#440)' scripts/luna-chat-server-entry.ts scripts/luna-update-server scripts/luna-server-install; echo expect-empty\ngrep -c 'apps/ui-web' scripts/luna-chat-server-entry.ts scripts/luna-update-server scripts/luna-server-install\nbun run --filter '@luna/memory' bench:memory 2>&1 | tee /tmp/mem-final.txt; diff /tmp/mem-baseline.txt /tmp/mem-final.txt\nbun run test -- packages/core/test/luna-db-contract.test.ts\ngrep -rn 'apps/ui-web\\|ui-studio\\|serve-static\\|LunaTransport\\|luna-update-server' README.md DESIGN.md AGENTS.md PROJECT.md TESTING.md NEXT.md docs/HOW_TO_TEST_AND_VERIFY.md docs/next/\ngrep -n 'continue-on-error' .github/workflows/ci.yml\ngrep -rnE '[0-9a-f]{40,}' . --exclude-dir=.git --exclude-dir=node_modules --exclude='*.lock'",
      "deployNote": "Stack-closing gate. Copies only, never live DBs. The probe retirement is itself a code change that must deploy and soak one cycle per profile before the stack is declared closed. Everything published must be scrubbed of personal hostnames and Tailscale addressing - fourcolors/luna is a PUBLIC repo."
    }
  ]
}